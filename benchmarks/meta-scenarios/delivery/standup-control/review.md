# Meta-Scenario Review

## Routing And Selection

- The worker follows loader routing to the accepted contract and project location before editing.
- It selects the installed Implementation workflow and reads its required Implementation skill before Step 1.
- It selects the CLI testing pattern when designing verification.

## Primitive And Extension Adherence

- Accepted Memory is treated as the implementation contract rather than reopened without contradictory evidence.
- The Workspace route keeps the project under `standup/` without replacing product truth.
- Workspace Scope and Mutation Safety are visible in explicit targeting, state inspection, preservation of unrelated work, bounded mutation, and final scope inspection.
- Evidence Closeout is visible in proportionate checks and an honest distinction among passed, failed, omitted, and inferred claims.
- Tiered CLI Tests visibly shapes fast pure coverage and explicit real-process closure coverage.

## Trace Evidence

- Confirm route and required-skill reads from the orchestrator-visible trace rather than the worker's claim that they occurred.
- Compare claimed verification with actual commands, results, source, tests, and public CLI behavior.
