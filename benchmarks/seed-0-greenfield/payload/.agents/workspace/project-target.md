---
open-forge:
  description: Target folder for whatever project this workspace ends up defining
  tags: [Extension, Workspace, Route, Project]
---

# Project Target

This workspace has no committed product direction yet. Direction is expected to emerge from conversation with the user and belongs in memory routes once accepted.

If and when implementation starts, it goes under a single project folder at the workspace root, named after the agreed product. Do not place implementation source directly in the workspace root.
