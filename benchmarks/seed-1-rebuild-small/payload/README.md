# Bookmarks CLI Workspace

This workspace is a clean restart after a rough MVP of a small local bookmarks CLI. There is enough recorded knowledge to guide the next build, but no production code to preserve.

Project truth lives in the Open Forge routes under `.agents/` — start from `AGENTS.md` and follow the loader. The vision, accepted decisions, prior learnings, and expected patterns are all routed; nothing important lives only in this file.
