# Historical Benchmark Results

These hand-authored reports predate the current scenario and dual-review protocol. They are retained as raw design history because independent review found useful product defects, worker overstatements, and framework friction in them.

They are not current run inputs, comparable replications, or causal evidence. Legacy local artifacts may still exist under the ignored `benchmarks/runs/` directory; the current runner requires a non-overlapping external runs root.
