Prepare `ledger remove <id>` for implementation. The local personal ledger has a pure TypeScript core, a `node:http` JSON server, and a CLI client. The server owns persistence and business rules; the CLI renders server results. Settle the removal behavior, produce the most concrete verifiable plan the available project information supports, and identify source inspection still required before implementation. Cover core behavior, server API, CLI help and behavior, storage, summary effects, errors, tests, and documentation. Do not implement the ledger. I am available for concise questions.
