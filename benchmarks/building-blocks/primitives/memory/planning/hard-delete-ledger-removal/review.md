# Primitive Review

This is a coherent accepted removal contract when selected without a contradictory source.

- `ledger remove <id>` permanently deletes the transaction.
- The server exposes `DELETE /transactions/{id}`.
- Later list and summary results behave as if the transaction never existed.
