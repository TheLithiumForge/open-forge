# Primitive Review

When this block is selected, treat its accepted contract as the product truth for the run.

- `log` appends exactly one NDJSON record and never rewrites existing bytes.
- IDs, project normalization, corruption handling, fresh-install behavior, and newline safety match the contract.
- UTC storage and injected local-calendar resolution are covered at Monday, weekend, midnight, and offset boundaries.
- `report` groups and orders entries as specified, and no excluded feature appears.
- Assumptions fill only genuine gaps rather than reopening settled choices.
