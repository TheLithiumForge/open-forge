# Primitive Review

When selected, every reported export fact should come from this accepted record.

- Compatibility includes UTF-8 NDJSON, physical-line boundaries, field order, record ordering, and unchanged default bytes.
- The optional inclusive UTC-date filter, read-only behavior, corruption failure, and atomic destination boundary are accurate.
- Non-goals and all seven verification obligations are present without unsupported additions.
- The record is named as the authoritative source.
