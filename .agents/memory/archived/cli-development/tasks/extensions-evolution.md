---
open-forge:
  description: Track completed Extension content naming, consumer permission, review corrections and full acceptance
  tags: [Memory, Archived, Contextual, Historical, CLI, Task, Extension, Evolution, Content, Destination, Complete]
---

# Task 24: Extensions Evolution

## Task State

- State: Complete, accepted and integrated into local develop.
- Permanent mapping: Task 24 “Extensions Evolution” in the
  [project control ledger](../project-control.md).
- Parent: [Complete The Replacement CLI](00-cli-development.md).
- Phase 5/5, milestone 8/8: `[########]`. All implementation, review, correction
  and full managed/native acceptance milestones are complete.
- Owner: root Overseer, completed sequential acceptance; no active subagent. The read-only
  `extensions_m7_review` reviewer completed using GPT-6 Astra/high.
- Current authority: the user accepted `content/`, an allowlist and a CLI question
  for missing grants. The revised [functional draft](extensions-destination-proposal.md)
  was presented before contract implementation. Earlier manual-only wording is superseded.

## Execution Capsule

Profile: streamlined assured at the material destination-admission and durable
permission boundary, using one continuous direct implementation owner. This is
an ordinary local developer tool handling consumer files, including uncommitted
work that Git cannot restore. Preserve exact ownership, no-follow containment,
lease revalidation, bounded effects and truthful partial results. The standard
BCL, existing typed interactive session, strict JSON and mutation/recovery
foundations supply the mechanism; exceptional machinery is none.

Five phases and eight fixed milestones:

| Phase | Milestones | Boundary                                                                                           |
| ----- | ---------- | -------------------------------------------------------------------------------------------------- |
| P1    | M1         | Freeze content naming, allowlist and interaction/lifecycle contracts and design.                   |
| P2    | M2, M3     | Freeze callable contracts, then decisive Red evidence.                                             |
| P3    | M4, M5, M6 | Apply content rename; implement permission foundation; integrate Extension consumers and recovery. |
| P4    | M7         | One fresh whole-task review and grouped correction.                                                |
| P5    | M8         | Full managed/public and supported linux-x64 Native AOT acceptance and integration.                 |

Review budget: one `T24-R1`, consumed by `extensions_m7_review` on candidate
`233a180d31d70d0ce94b1082a1400e6c2e398497` against accepted base
`a157d439a8f130ef80ad7a87b0d3fcc6b7440d4d`.
Grouped correction: one `T24-C1`, consumed for the three accepted findings below.
Writing review: one `T24-WR1` for the changed durable contract/design pack,
consumed by read-only `extensions_contract_review`, GPT-6 Astra/high; council budget zero. Optional helper work must have one bounded scope.
Source and shared artifacts remain serialized. Do not create a parallel mutable
implementation lane from the user’s permission to use occasional subagents.

Pure grammar/admission uses Unit evidence; persistence, prompt streams and
copy/link/ownership effects use focused Integration evidence; the six commands must finish with exactly three simple public journeys each.
Live M4 discovery found 38 older public cases; M6 must consolidate them to 18
while retaining required Open Forge behavior evidence at lower tiers. Freeze affected
consumers and source hashes before gates. The public layout, shared permission,
serialization and mutation changes trigger full managed and linux-x64 Native
AOT acceptance at M8. Task 25 remains separate; Task 26 consolidates behavior
only after both functional tasks are accepted.

## Accepted Boundary

The accepted Extensions Evolution idea is contextual input for a future local
and offline Extension boundary. Task 24 preserves two explicit decision fronts:

- apply the user-accepted atomic `payload/` to `content/` package rename;
- define consumer-owned permission for exact workspace-relative Extension copy
  destinations beyond `.agents/` without allowing package metadata or source
  bytes to grant themselves access.

The user accepted `content/` and a consumer allowlist with a CLI question for
missing grants on 2026-09-08. The revised functional draft defines the proposed
exact-file flow. M1 promotes the reviewed permission contracts and content
layout into the permanent CLI routes. The new contracts define the accepted
non-shipping behavior; implementation remains pending. No compatibility layer
follows from the rename.

Extension permission, ownership, update, removal, collision, recovery, and
security remain one Extension-local lifecycle. Task 25 separately owns any
future Library projection outside `.agents/`; matching destination mechanics do
not merge copied-file and live-link ownership. A neutral path-admission
primitive may be shared only after both consumers prove identical meaning.

The source-reader fail-closed and Update parent-catalogue candidates discovered
by the narrower preparation return to this future behavior-contract review.
Task 26 cannot use them to change behavior during internal consolidation.

## Implementation Conditions

The remaining command sequence is integrated. The user reviewed the functional
direction and accepted the folder rename, consumer allowlist and missing-grant
question. Sequential contract preparation and implementation are authorized.
The fixed horizon and capsule above replace the earlier preparation-only state.
Freeze the complete contract and design pack before Gray, Red and Green.
No compatibility machinery, runtime registry, remote action or publication
follows from this authorization. Task 25 retains its own unresolved external
source-selection choice; it does not block Task 24. Task 26 follows both tasks.

## Architecture Preparation

The completed read-only architect `destination_architecture` used
GPT-6 Astra/high. Its follow-up checked the actual recovery contracts and
retracted an initially proposed automatic permission-restoration Repair entry.
The shared mutation contract requires recoverable pre-effect evidence, while
Repair deliberately implements a finite catalogue. Preserve the prior permission
file or prior absence in the existing operation bundle; permission restoration
remains an explicit manual edit. Task 24 needs no new Repair production operation.
Task 25 must exclude this control file from generic Library residual repair
attribution so that link recovery cannot silently restore or revoke permission.

The root personally read the complete current C# directive trio before design:
`_csharp.md` SHA-256 `31045ebcb02d5bfeee8ba9f3112d307b1f72a2186cda618fbf7d22e7d1d90b53`,
`design.md` SHA-256 `76aa8fc7aaaa79d9535998f5557150f3754e3d80520a7a06864b61659373c1a9`,
`style.md` SHA-256 `c3fa9d31575e77fedb103ca397f0ccf10ab7236e6edbef1658c7fe36138457cb`.
The [preparation draft](extensions-evolution-contract-draft.md) retains the
reviewed design input. Current semantic authority is the
[Workspace Permissions contract set](../../../crystallized/documents/cli/contracts/shared/workspace-permissions/_workspace-permissions.md),
the changed Extension command contracts, and the
[shared Technical Design](../../../crystallized/documents/cli/technical-designs/workspace-permissions.md).

## Functional Draft

The [joint functional proposal](extensions-destination-proposal.md) presents
recommended package vocabulary, consumer permission, concrete copy and link
examples, lifecycle behavior, and the remaining user decision. It is contextual
and must reach the user before implementation.

## M1 Writing Review Disposition

`T24-WR1` reviewed draft SHA-256
`a012f9ecf10f220d9ec2542102bd538a8c4cf70f6933ceaae9472bc8c1361ac7`
and returned three findings. Root accepted the two permission representation
findings. The user subsequently declined legacy handling for the pre-public
project, superseding the proposed old-folder diagnostic:

- `T24-WR1-001`: user-directed disposition: update our own Extensions to
  `content/` and add no legacy detection, diagnostic, migration or compatibility
  behavior. Preserve existing empty-package semantics. The diagnostic proposed
  during review was removed before implementation.
- `T24-WR1-002`: separate planned permission action from observed application
  outcome, including verification failure and unknown completion; use existing
  ordinary-file receipts and retain observed outcomes after later failures.
- `T24-WR1-003`: represent permission determination as `not-evaluated` until
  complete; reserve `not-required` for a proven empty requirement set.

This closes the writing review's corrections, not M7 whole-task review. The
reviewer independently reported the complete C# trio fingerprints above.

## M1 Scope And Next Boundary

The branch `codex/extensions-evolution` starts from accepted integrated baseline
`a157d439a8f130ef80ad7a87b0d3fcc6b7440d4d`. Root remains the sole mutable owner.
Expected production scope is Framework Permissions and existing Extensions
source/embedded capabilities, Extension Create/Inspect/List path values and
Install/Update/Remove permission planning/application/results, their focused
Unit and Integration tests, eighteen existing public journeys, and first-party
Extension content assets. Root composition changes only if existing typed
interaction needs a directly required connection.

Protected: unrelated commands, Library behavior until Task 25, projects and
dependency/toolchain pins, legacy CLI implementation, sealed evidence and
handoffs, unrelated worktrees, global installation and publication. The source
reader enumeration candidate must reproduce in Red before a correction; do not
turn unavailable source classification into an empty/valid package. Any Update
parent-catalogue concern must name a concrete failing contract before change.

M2 freezes immutable permission facts, strict codec/evaluator/observer callable
surfaces and command result additions. M3 freezes decisive Unit and real-filesystem
Integration evidence before Green. Exact production declarations remain in
source; prose owns semantic promises. M4 applies only the current content rename,
M5 implements permission mechanics, and M6 connects the three Extension mutations.

## M1 Validation

The coherent Markdown pack passed the local formatter. This worktree's published
CLI indexed the new shared permission route, Technical Design, Extension route
and task draft with no findings; apply changed five generated regions and the
repeat changed none. The local link scan found no newly introduced broken link.
Two pre-existing links in the historical Extensions MVP architecture point to
absent archived observations; their unchanged baseline text is retained.
No production build or behavioral acceptance is claimed for this prose boundary.
`artifacts/task24-m1/` retains exact Index output and the fresh input freeze.

## M2 Callable Gray

M1 is immutable commit `a65159b097a4aa7b926bab53371727a423a1397f`, tree
`8a219131daa9acb98b24860e6a8f777cbd53597f`. M2 adds thirteen permission
source files and changes eleven existing command result/presentation files.
Immutable document, subject, requirement, observation, approval and result
facts live in cohesive Models scopes; reader, codec, evaluator, change planning
and receipt projection live under their capability's Shared scope.

Seven behavior method bodies deliberately throw `NotImplementedException`.
The concrete command-local wire shape and pure exhaustive enum projection are
implemented as callable contract formation. Install, Update and Remove carry
the same immutable permission facts through their result and JSON graphs.
Early terminal results say not-evaluated; this does not implement permission
admission or change current mutation ownership.

All 24 affected C# files passed the 200-character line and named prohibited
pattern checks. Informational formatting ran actual analyzers over the initial
sixteen files and then the eight concrete wire files. It applied collection
expression style fixes in the touched result/rendering files. The complete
post-format same-worktree build passed with zero warnings and errors. Three
duplicate-ID JSON smoke requests exited 4 with empty stderr and the exact
not-evaluated permission shape. They made no workspace effects.

`artifacts/task24-m2/` retains the earlier contract-only builds, both formatting
passes, final `build-wire` logs, three wire smokes and the final source freeze.
There is no full behavioral or Native AOT acceptance claim at this boundary.

## M3 Decisive Red

M2 is immutable commit `7b637a5c46b1ee6ced16b6eb17bee63d36e0d0a5`, tree
`920b7711165061893cd11c15040c478c0b260d88`. M3 adds ten test/fixture files and
updates eight existing test files for the accepted finding and result contracts.
No production implementation changes at this boundary.

The final focused run selected 76 Unit cases (37 pass, 39 fail) and 53
Integration cases (19 pass, 34 fail), with zero skips. The 39 Unit failures
comprise 36 intentional Gray exceptions and three obsolete finding catalogues.
Integration failures comprise eight unimplemented observer cases, two current
source-reader assertions and 24 permission/admission assertions. The latter
include three revised pre-lease destination cases. All failures reached the
intended method or operation assertion; none is a fixture/disposal exception.
The new post-grant failure/recovery assertions remain behind earlier missing
behavior and must execute fully during Green; Red does not prove those later
postconditions yet.

The source enumeration case reproduces a root containing both a manifest and a
child package, with directory listing denied. Current classification returns
Complete; the accepted source contract requires Unavailable. M4 may correct this
specific false-success path. Update revocation currently reports
projection-unavailable and Remove reports target-outside-agents; neither
performs the required permission determination.

The corrected full solution build passed with zero warnings and errors. Initial
build attempts separately exposed missing restore assets and a Unix API callback
without a compiler-visible platform guard. Locked restore and a direct guard
resolved these; they are not behavioral Red evidence. An invalid internal-wildcard
test filter was corrected before the final run. Informational formatting ran
actual analyzers on all eighteen changed C# files and checked the corrected
callback separately. Runtime hashes remained identical through both final runs.

`artifacts/task24-m3/` retains all attempts, final source/runtime freezes,
commands, raw output, CTRF results and classified failures. Public journey
counts remain unchanged; full managed and Native AOT acceptance belongs to M8.

## M4 Current Content Layout

M3 is immutable commit `0ab5460fdf65133d5f1dc5ef5d341d866a6c2a58`, tree
`6554854eabe6ec226f1894f140c78658c19bbe3f`. M4 moved all 21 first-party package
files from `payload/` to `content/` without changing their bytes. The package
README now uses the replacement CLI install command and lifecycle filename.
The embedded inventory and canonical compressed archive were regenerated from
the current 24 authored assets, including that README and inventory.

The shared package layout constant now drives current source and embedded
readers and Create scaffolding. Current fixtures and source-path assertions use
content. Result nouns, recovery payload storage and effect-kind wire values keep
their existing meaning. No legacy detection, alias or migration was added.
The reproduced directory-listing exception now maps to source Unavailable at
classification, instead of falling through to package Complete.

The full solution build passed with zero warnings/errors after informational
formatting ran actual analyzers. The selected Extension run executed 215 Unit
cases (212 pass, three pending permission catalogue failures), 220 Integration
cases (200 pass, nineteen pending permission failures and one stale scaffold
ordering assertion), and 38 public cases (36 pass, two stale result-member
assertions). Both new content assertions and both embedded catalogue assertions
passed. The three stale expectations were corrected; the final warning-free
build and focused reruns passed 12 scaffold Integration cases and seven public
Install/Remove cases. Production bytes were unchanged by that test correction.
These scoped receipts close M4, not permission behavior or full acceptance.

`artifacts/task24-m4/` retains source/runtime identities, exact asset move hashes,
commands, raw output and CTRF results for both runs. Only the obsolete copied
package folder inside ignored Integration build output was removed. All source
moves and formerly untracked files are included in the new source freeze.

The live public inventory contains 38 cases across the six Extension commands,
not the intended eighteen. M6 must reduce these to three simple journeys per
command and preserve required detailed evidence in the proper lower tier before
M7/M8. The existing Task 23 Library journey count remains fifteen.

## M5 Permission Mechanics

M4 is immutable commit `fe0417a4c58ee05cbebf0788f13c322b48dc73dd`, tree
`cbf18b4b8d8b3b035a9ca631cd7625dcca5ed99f`. M5 implements all seven Gray method
bodies: strict UTF-8/schema decoding and canonical writing, exact grant
membership and additions, consumer-file observation, approved ordinary-file
change planning, and truthful receipt projection. No command grants or writes
permission yet; M6 owns those consumers and their result propagation.

The former Extension target parser moved, without grammar changes, to
Framework Filesystem Shared Paths as PortableWorkspacePath. Existing Extension,
lifecycle and Install result consumers reference the same neutral parser.
Permission membership and union use its exact invariant-lowercase portable key;
a regression distinguishes Greek final sigma from sigma so a broader ordinal
case comparer cannot silently merge distinct contract keys. Missing permission
parents remain absent, unsafe links are blocked, unreadable files are unavailable,
and invalid files retain their exact observation without becoming empty grants.

Library entries retain unique IDs and bind sourceRoot. The shared grant helper
refuses implicit rebinding of an existing ID to another root. Task 25 must define
its explicit Library source-rebinding flow before adding that consumer; no
Library command behavior changes in M5.

The original 53 permission Unit cases passed in the first build. Twelve
supplemental cases cover strict-byte input, duplicate/schema grammar, portable
identity, Library binding and all supported/undefined receipt mappings; these
are Green-stage evidence improvements, not retrospectively claimed frozen Red.
Two additional real-filesystem cases cover absent parents and unreadable files.

Final informational formatting and the corrected full solution build passed
with zero warnings/errors. The focused run passed all 65 permission Unit cases;
the expanded Unit selection passed 157 of 158, with only the frozen M6 Install
finding-catalogue failure. Integration passed all fourteen selected cases: ten
permission observations, two content-source cases and two embedded-asset cases.
No cases were skipped. Runtime hashes stayed identical through these runs.

The first post-format build exposed a formatter-induced static Normalize method
whose out-of-selection Unit caller still used an instance. Root completed that
small static conversion, removed its unused injected object and updated all
callers. No normalizer behavior changed. Both attempts remain in
`artifacts/task24-m5/`, with source/runtime freezes, commands and CTRF results.

Carson's bounded read-only follow-up checks M6's immutable plan/approval placement
against the actual three command pipelines while root freezes M5. This is
architecture preparation, not the separately reserved M7 whole-task review.

## Accepted M6 Integration Shape

Root accepted Carson's narrow recommendation after inspecting Install's actual
plan and application stages. Use a command-private immutable execution envelope
in each leaf's Models/Application scope. It contains the existing immutable
content plan and one cohesive permission stage, and derives complete file changes
and recovery targets once. Preserve existing content plan constructors and
cross-member invariants; do not copy their package/topology/lifecycle fields or
move all preflight into planning merely to attach approval.

Observe and ask after structural preflight. Move no-op exits after permission
determination. Under the same lease, validate the content plan through existing
mechanisms and separately compare exact permission bytes/absence and requirements.
Keep permission validation separate from positional content validation indexes.
Prepare one complete bundle, then write permission before the existing content
application entry. Carry its observed receipt through every later result path.

The old pre-lease destination assertion that all effects are empty must become
an assertion that none was applied when a complete plan now exists. Continue
asserting unchanged source/workspace/control bytes and no lease/recovery effects;
retain truthful planned or not-started content facts. This corrects an inherited
blanket-rejection oracle rather than relocating the permission question ahead of
complete preflight. No product scope or existing ownership protection changes.

The architect personally reread the current loader and complete C# trio and
reported the same fingerprints recorded above. No implementation, tests, builds
or additional agents ran in that follow-up. T24-R1 and T24-C1 remain reserved.

## M6 Extension Consumers

M5 is immutable commit `004ff5ac9751d4569a66c992a9153deed51f6fea`, tree
`8694fb7d538ef65c30d85b96e977ea5e1c54ec4d`. Install, Update and Remove now
carry command-private execution envelopes around the existing content plan and
one family-owned permission stage. Complete preflight precedes the single
question. The prompt names source identity when applicable, exact subject/path
pairs, planned copy/delete/release/preservation, and remembered approval.

Permission-only reapproval passes through the same lease and verified bundle.
Content validation keeps its existing positional checks; permission observation
is compared separately by exact bytes or absence. Install additionally rechecks
unchanged targets before saving permission. The consumer file is created or
replaced before content and lifecycle; its observed receipt survives later
failure. Human and JSON results preserve the same permission facts, including
known requirements when determination is interrupted or unavailable.

The ordinary-file applier now accepts any exact verified reversible Create
preparation instead of restricting that neutral mechanism to Library producers.
Recovery still requires the same operation and target match. No automatic
permission restoration or new recovery format was introduced. External files
use exact bytes and stay outside routing. Portable protection covers control
files, overwrite companions, Framework and Library claims, existing managed
aliases, and selected recovery storage; source disjointness remains mandatory.

The first broader run exposed two real gaps: outside-scope overwrite suffixes
were not protected, and the content revalidator rejected permission-only
Install as an empty mutation. Additional evidence now covers reapproval for
Install and Update no-ops, Update copy, Remove deletion, declines, unchanged
targets edited during approval, and independent initial-force authority. The
same run also found two redundant stale catalogue counts and a missing fixture
parent. A too-high Integration minimum and an unsupported public filter were
execution-selection errors, not behavioral evidence. All attempts remain
separate in `artifacts/task24-m6/`.

The corrected full solution build passed with zero warnings/errors. Final
focused evidence passed 288 Unit, 221 Integration, and eighteen public cases,
with zero failures/skips; runner artifacts stayed byte-identical through the
runs. The Integration selection includes the ordinary-file applier. The public
inventory is now exactly three cases for each of the six Extension commands.
Redundant help, parser, status, view and schema variations retain their existing
owned binding/presentation, application and scaffold evidence at lower tiers;
public command journeys remain process-based. Library's fifteen journeys are
unchanged. This closes M6, not M8 full managed or Native AOT acceptance.

Root personally reread the complete current C# trio and reconfirmed the
fingerprints recorded above. Informational formatting ran actual analyzers on
59 affected C# files and a focused correction pass. The final build proves the
replacement for the old nullable test suppression through `Assert.NotNull`.
`T24-R1` remains reserved for the immutable whole-task review, followed by the
single grouped correction `T24-C1` if required.

## M7 Review And Grouped Correction

`extensions_m7_review` reviewed candidate `233a180d31d70d0ce94b1082a1400e6c2e398497`
against the accepted Task base and personally reported the unchanged C# trio
fingerprints recorded above. Root accepted three source-traced findings, pending
focused filesystem reproduction and correction under `T24-C1`:

- `T24-R1-001`, destination safety: Remove and retired Update paths can follow a
  contained parent link into protected storage. Reject linked ancestry for every
  selected current/intended destination, including ownership release, before
  approval and during lease revalidation.
- `T24-R1-002`, control-path protection: reserved file identities can be used as
  destination ancestors. Reject portable exact identities and descendants so a
  package cannot create a directory at the permission control-file path.
- `T24-R1-003`, direct consumer consistency: Inspect still interprets external
  Markdown semantically although Install/Update persist exact-byte identities.
  Keep external current/intended fingerprints and generated-marker facts opaque,
  with the existing internal Markdown semantics unchanged. Align Inspect prose.

The reviewer ran no builds or tests. M6 focused receipts remain historical green
evidence; these findings invalidate their sufficiency for acceptance. M8's full
managed/native execution follows the correction and fresh source freeze.

The final correction build passed with zero warnings/errors after actual
informational formatting. Focused evidence passed 292 Unit, 230 Integration
and eighteen public cases, with zero failures/skips and unchanged runtime
artifacts. All three findings are fixed and verified. External Inspect now
compares exact identities without generated-region facts; its semantic-only
action recommendation gate remains unchanged. The final scan also replaced
six inherited test null suppressions with compiler-proven checks. All 130
affected C# files pass the line-length and prohibited-pattern scans; protected
configuration and Library production paths are unchanged.

`artifacts/task24-c1/` retains review identity, initial failing reproductions,
format/build attempts, final source/runtime hashes and CTRF receipts. The two
additional plain-Markdown cases were added during correction; they are not
retrospectively claimed as frozen Red. M7 is complete; M8 runs the full managed
suite, native CLI and native Integration/public suites plus managed-on-native
public execution from one corrected source candidate. No global refresh or
publication is included.

## M8 Full Acceptance

The final source passes 2,719 managed Unit cases, 1,518 managed Integration
cases and 188 managed public cases. Native linux-x64 Integration passes 1,518,
native public passes 188, and the managed public runner against the same native
CLI passes 188. All failures/skips and compiler warnings/errors are zero.
The full informational formatting verification reports zero diagnostics and
zero changes. Source and runner identities stayed fixed during each gate.

The first full formatting selection exposed informational diagnostics in 22
already-touched files. The formatter reported affected files but could not apply
those analyzer fixes; Git confirmed unchanged bytes. Root corrected concrete
collection declarations, direct indexed access and exception parameter names,
then reran formatting, the full build, and every managed/native gate. Supported
command behavior and public graphs are unchanged by those corrections.
All attempts remain separate in `artifacts/task24-m8/`.

The [canonical acceptance receipt](../../../../../artifacts/task24-m8/acceptance.json)
records exact commands, source/runtime manifests and gate counts. Unit discovery
has 2,697 identities: matching runner metadata proves that nine unchanged Find
methods expand by 22 cases during execution. The runtime public inventory proves
three journeys per Extension command and fifteen Library journeys. All 130
affected C# files pass static checks; all 24 previously recorded unrelated dirty
files retain their bytes and modes. No global refresh or publication occurred.

The final accepted source includes M7 commit `e541d574` plus the inspected
analyzer corrections above. A forward feature commit freezes this receipt and
current state before the accepted task is squash-integrated onto `develop`.
Task 25's source-selection decision remains open; Task 26 stays later pure
refactoring. No legacy handling was added for the content rename.

## Accepted Local Integration

Feature candidate `3802fb039593c87fbb68a7888c2928e4877ad828` was squash-integrated into local `develop`
at `2eedaf872a48c8eb3e80f7feb50bea8e69cfd638`. Both have tree `c56ea6b9b17dca7ae9f12b29903c78ddd07be580`. The complete Git tree, all 3,120
source bytes and their executable modes match. Ordinary checkout normalized
24 non-executable permission modes; this is recorded separately from Git mode
identity in `artifacts/task24-m8/integration.json`. The initial overly strict
POSIX-mode check stopped on that normalization; the complete subsequent
comparison confirmed the accepted tree and content. No source repair or history
amendment was needed. Full execution receipts remain valid for the identical
source; this forward state update changes prose and acceptance metadata only.

Task 24 is complete. Task 25 awaits the user's external source-selection choice.
