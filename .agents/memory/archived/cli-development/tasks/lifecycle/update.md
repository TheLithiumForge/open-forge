---
open-forge:
  description: Implement root Framework update from accepted lifecycle identity
  tags: [Memory, Archived, Contextual, Historical, CLI, Task, Update, Framework, Lifecycle]
---

# Task 6: Root Update

## Task State

- State: Complete. The accepted Green, grouped correction, squash integration,
  and post-integration evidence are complete. The exact immutable lineage and
  accepted integration are recorded in the closeout below. The clean
  provisional branch `codex/root-update-provisional` remains preserved at
  `69a1bd83`, tree `2a10f683`, from obsolete base `0d269b7a`; its Gray and Red
  material is evidence for revalidation only and has no acceptance authority.
- Current progress: phase 5 of 5, milestone 8 of 8. The streamlined phases are
  Preflight, explicit Gray/Red, one coherent implementation and focused-
  verification pass, one fresh whole-task review with at most one grouped
  correction, and acceptance. The milestones are Preflight, Gray, Red,
  coherent production, focused/full evidence, fresh whole-task review, grouped
  correction or documented no-op, and acceptance/integration.
- Permanent mapping: Task 6 “Root Update” in the
  [project control ledger](../../project-control.md).
- Status/Doctor obligation: extend the typed producer-attribution and affected
  evidence inventory through the existing Framework lifecycle and recovery
  contributors before Task acceptance. Update creates no seventh durable domain
  or contributor.
- Parent: [Lifecycle Commands](_lifecycle.md).
- Contracts: [Interface](../../../../crystallized/documents/cli/contracts/update/interface.md) and [Behavior](../../../../crystallized/documents/cli/contracts/update/behavior.md).

## Activation Capsule

- Profile: streamlined assured trial. Preflight must revalidate the complete
  accepted Update contracts and may select a larger profile only for a named
  material risk.
- Ownership: Sagan is Task Mastermind and supervises every specialist. No C#
  author or reviewer acts before personally reading and fingerprinting
  `_csharp.md`, `design.md`, and `style.md`. After accepted Gray and Red, one
  Brilliant Implementer owns coherent Green through focused verification.
- Current action: retain the accepted integration and report the complete
  remaining task queue. Do not activate Task 17 or any later task at this
  boundary.
- Experimental overlap: provisional command-private Gray and Red may be
  prepared in isolation. Any accepted shared foundation, cross-command meaning,
  root composition, Status/Doctor producer change, and semantic Green remains
  serialized in dependency order. No generic lifecycle, mutation, planning,
  registry, or command engine is inferred from similarity among later commands.
- Evidence boundary: tests cover only Open Forge-owned request, lifecycle,
  comparison, planning, recovery, mutation, verification, result, and public
  process behavior. Third-party library, runtime, operating-system, and test-
  platform internals are out of scope. Public evidence stays at three simple
  Update journeys unless Preflight identifies a material public-behavior gap.
- Expected Task-local paths are `src/cli/core/OpenForge.Cli.Core/Commands/Update/**`,
  focused Unit and Integration mirrors, and bounded public Update evidence.
  Root composition, help, embedded-payload, lifecycle, generated-navigation,
  recovery, Status, and Doctor neighbors require exact assignment before change.
- Budgets: council `0`; one fresh whole-task review `T6-R1`; one grouped
  correction `T6-C1`. Gray and Red remain distinct frozen boundaries and do not
  consume those budgets.
- Prohibited expansion: no dependency injection, service locator, reflection,
  runtime registry, generic operational engine, compatibility machinery,
  JavaScript/MJS/CJS implementation, remote action, destructive external
  action, publication, or release.
- Stop before accepting stale provisional evidence, weakening trusted lifecycle
  or recovery rules, overwriting unrecognized divergence, inferring ownership,
  introducing cross-command policy, changing public meaning without maintainer
  authority, downloading a dependency, or relying on a stale or zero-test gate.

## Accepted Preflight Freeze

- Identity and disposition: the clean activation baseline is `0bc82357`, tree
  `41b74d9c`. The provisional branch remains clean and untouched at Gray
  `ed614488` and Red `69a1bd83`, tree `2a10f683`. Neither commit is
  transplantable. Definitions, binding, request, and force/prune scenario intent
  may be reauthored; incomplete comparison, planning, result, and evidence
  surfaces must be rebuilt.
- Architecture: add no new shared core and depend on no sibling-private Install
  or Route implementation. Consume the existing neutral payload reader and
  projector, Framework content identity, lifecycle reader/planner, generated-
  navigation formation/projector, workspace lock, mutation validation/applier,
  and recovery store/deletion guard. Update owns historical-provenance
  classification, complete prior/current/intended facts, retirement and
  authority policy, whole-plan blocking, coalesced ordering, findings/result,
  recovery mapping, lifecycle-state formation, rendering, and orchestration.
- Historical source rule: a structurally valid recorded non-null
  `sourceAssetPath` absent from the current payload is a command-local retired
  comparison fact, not a shared lifecycle-source error. A derived generated
  region has null source provenance and uses exact-byte identity.
- Callable freeze: retain `UpdateSymbols.Create`; bind one immutable
  `UpdateRequest`; freeze pure
  `UpdatePlanningPolicy.Plan(comparisons, authority)`;
  `UpdatePlanBuilder.Create().BuildAsync(request, cancellationToken)` reads
  fresh complete facts; `UpdateOperationFactory.Create` takes typed
  `interactiveSession` and `lockStoreRoot` inputs; and its `ExecuteAsync` takes
  `request` and `cancellationToken`. The bounded Gray addendum freezes
  `UpdateRecoveryDeletionMapper.Map(deletion)` and its typed outcome. Gray may
  throw only at the four named intentional Red seams. Green explicitly
  constructs dependencies.
- Plan rule: one blocked selected fact blocks every effect. `Preserve` is not an
  effect; a plan containing only no-op and preserve decisions is effect-free.
  Each physical path has one coalesced effect, application receipt, protected
  path, and recovery entry with a nested ordered logical change list; authored
  and generated changes sharing a file never become competing physical writes.
  Automatic grants neither force nor prune. Dry-run shares planning and
  preflight but acquires no lease and creates no bundle or write.
- Interaction and public result: the exact one-confirmation matrix, ordered
  schema-v1 Update result, 30 finding codes, finite values, ordering, and next
  actions are frozen in the accepted Update Interface and Behavior contracts.
  The public finding object is exactly `{ code, target, cause }`; unavailable
  workspace selection is blocked. No compatibility alias or machinery is
  authorized.
- Status and Doctor: Update produces no independent durable domain. Its final
  Framework lifecycle and recovery facts remain owned by the existing two
  typed contributors. Evidence must prove Update's frozen
  `framework`/`update`/`workspace` recovery attribution, unchanged six-member
  contributor catalogue, and affected Status/Doctor projection without new
  vocabulary. Exactly three Doctor public journeys remain unchanged.
- Gray evidence: freeze 15 green contract cases: five grammar/request, five
  validated comparison/provenance representation, and five result/JSON/finding/
  next-shape cases. Result evidence includes two logical authored/generated
  changes sharing one physical effect. Gray contains declarations and
  intentional callable seams, not planning or application behavior.
- Red evidence: freeze exactly 45 intentional failures: 20 Unit behavior cases,
  22 real-workspace Integration facts, and exactly three published Update
  journeys. Unexpected pass, a non-45 selection, any skipped required case, or
  zero selection stops Red. The three public journeys combine truthful help and
  invalid/idempotent grammar, safe historical preview/apply/repeat, and normal
  divergence followed by bounded force/prune/repeat plus Status/Doctor
  observation. The generated-projection Integration fact proves one physical
  effect, receipt, protected path, and recovery entry when logical authored and
  generated changes share a file. No fourth Update journey is authorized
  without a material gap.
- Evidence and budgets: focused warning-free Unit, Integration, and EndToEnd
  evidence precedes one coherent Green; final acceptance requires formatting,
  static, protected-path, callable-shape, prohibited-pattern, line-length,
  durable host-path, full managed/public, managed-on-native, and supported
  `linux-x64` Native AOT gates. Council remains `0`; whole-task review `T6-R1`
  and grouped correction `T6-C1` each remain at most one.
- Experimental observation: isolated provisional Gray/Red exposed grammar and
  the force/prune matrix early, but its stale base and incomplete global seams
  made commit transplant unsafe. Continue parallel read-only discovery,
  command-private Gray/Red drafting, and isolated mechanical gates. Serialize
  shared meaning, composition, public schema, Status/Doctor integration, and
  semantic Green. No elapsed-time or numeric speedup claim is made.

## Accepted Gray Freeze

- Identity: commit `ef584350a48d08b2d6307eea70f87f2f3c46283a`, tree
  `f49f4be76607aec8d03d72252600e6c6718bc6a5`, parent
  `48ac549c241e769246cfecb773124ccbc5076dfa`. The clean commit contains exactly
  13 production and three Unit paths; the newline-sorted path SHA-256 is
  `48b79d619964dd0f4a69b0401a8bcdd1eb1021eafb0057e85db542156d4d20b1`.
- Surface: the accepted grammar, immutable request, comparison/provenance,
  force/prune authority, physical-effect and nested-logical-change result,
  schema-v1 JSON graph, 30 findings, next actions, operation factory, and three
  intentional callable seams are frozen. Planning, plan building, and operation
  execution remain the only intentional `NotSupportedException` seams for Red.
- Evidence: exactly 15 `Feature=update` Unit contract facts selected, discovered,
  executed, and passed with zero failures or skips. Forced Release Core and Unit
  builds passed with zero warnings and errors. Formatting, analyzers, host-path,
  prohibited-pattern, line-length, protected-surface, and diff checks passed.
- Corrections before freeze: exact finding causes are preserved; exact-byte
  fingerprints bind their bytes; baseline nullability is equivalent to a
  genuinely new comparison; logical and physical effects enforce nullability,
  identity, uniqueness, and canonical order; and every public collection is
  normalized at the production boundary.
- Experimental observation: parallel read-only Preflight and isolated
  mechanical gates reduced dependency idle time, while supervised pre-edit and
  semantic checks prevented an oversized microtype split, one duplicate
  definitions path, and a false-green representation freeze. Lost or delayed
  child receipts caused avoidable pauses; separating exact mechanical evidence
  from semantic authoring restored observable progress. No reliable elapsed-
  time baseline exists, so no numeric speedup is claimed.

The bounded Gray recovery-deletion addendum is commit `7a9ded305e9ee185c02aaf636b4ff78f301b1fb1`,
tree `d2ebd52378095775897bbb64c59eebfef68cc0cf`, parent `c7f5097458899ab98ff312a1b2c024a559a623a2`.
It adds exactly one command-private production path and one named intentional
Red seam for typed Removed, Retained, Unknown, and Interrupted recovery-deletion
mapping. It changes no shared Framework callable, public schema, or runtime
composition.

## Accepted Red Freeze

- Identity: commit `13fe18a9d1cc9f829cc0cf778c44c96f97abddcb`, tree
  `a834c59be9cc1c5409ee8502ac951ca02030fe40`, parent
  `7a9ded305e9ee185c02aaf636b4ff78f301b1fb1`. It contains exactly eight test
  paths: three Unit, three Integration, and two EndToEnd. The newline-sorted
  path SHA-256 is
  `6c71099a8f0f32fa3c0979c74d608119d6ddb09827f12bd7c36029953ee9daf7`.
- Evidence: forced Unit, Integration, and EndToEnd builds and the managed
  development publication passed with zero warnings and errors. The Unit
  selection executed 35 cases: 15 accepted Gray passes and 20 intentional Red
  failures. Integration executed and intentionally failed 22/22; Update
  EndToEnd executed and intentionally failed exactly 3/3. No required case was
  skipped. Failures reached only the four frozen seams or the unavailable root
  `update` command.
- Safety meaning: retirement uses one real historical target absent from the
  current payload; prompt-time mutation is blocked by complete-plan
  revalidation before recovery preparation; one bundle protects exact replace
  and delete paths; Extension lifecycle state is compared semantically; invalid
  lifecycle and provenance are isolated; end of input interrupts without lease
  or recovery; and one physical effect owns coalesced authored and generated
  changes.
- Static evidence: full formatting, focused analyzers, untracked diff,
  protected-path, callable-shape, prohibited-pattern, host-path, fact-count,
  four-seam, and line-length gates passed. Exactly three Doctor EndToEnd
  journeys remain unchanged.
- Experimental observation: splitting small semantic Red slices among cold
  owners produced no useful output before the stop rule fired. Reusing one warm
  context produced the files, but terse first-file checkpoints were necessary
  for visibility. Central Task Mastermind reconciliation rejected false
  retirement, revalidation, recovery, and Unit-tier evidence before builds;
  mechanical gates then exposed fixture ownership and publication setup errors
  without semantic ambiguity. Retain parallel read-only and exact mechanical
  work, but keep shared/public Green serialized under one owner. No numeric
  speedup is claimed.

## Accepted Closeout

Task 6 “Root Update” is Complete at phase 5/5, milestone 8/8. The accepted
immutable lineage is:

- activation `0bc82357a4fc7bd54ecbca1d58501d721c04baa6`;
- Preflight `48ac549c241e769246cfecb773124ccbc5076dfa`;
- Gray `ef584350a48d08b2d6307eea70f87f2f3c46283a`;
- bounded Gray recovery-deletion addendum
  `7a9ded305e9ee185c02aaf636b4ff78f301b1fb1`;
- Red `13fe18a9d1cc9f829cc0cf778c44c96f97abddcb`;
- coherent Green `a59d4df80a1f5d23cd7848140d7462495ca0a77b`, tree
  `d4a530e785dadd5e899fc73a410aec228b536201`;
- logical grouped `T6-C1` physical follow-up
  `c03c057cbc5fe204ce115ef4c4001968b27df04c`, tree
  `01769edbff77400bb54507a1d694d68015c29210`,
  `a252890756d166169c3d5b9bbd8a7adaa62cb896`, tree
  `b55f15814e5f5153f0cbc37ce812b7bd56fa8a68`, and
  `2e6b669d9ec2f8ce6fda7e176c1ba13d913cd9ba`, tree
  `893160afdc52ac5d7fac966cef1e1f308da4817b`;
- separately exposed full-gate direct-consumer evidence correction and final
  candidate `ac96f4cc57550a83ef8651b40869ae4ff35da34e`, tree
  `195f15388d6251f69244946183209d3dfe86b24a`. This correction changes Install
  help evidence only and adds no runtime behavior. The accepted candidate was
  squash-integrated as `c6eec9d26ad6b798d418d260027241795fb4aefc`, with exact tree
  `f31cacb0c54bda8ecf8a91d3516c6ffa48f48753`, from `develop` parent
  `d0d475f3b8106dfa7c8552cabab4c197bad53a71`, tree
  `c3d621989a4b6698d5a9a8de1505da16a750dbcb`.

Integration adoption covered 65 paths. Its sorted-path SHA-256 is
`88cbcb641e9a164fa61de9e357a26cf7f007a265b2b7686460d3e8f324ec5dcd`; the
expected and actual pre-commit tree is
`f31cacb0c54bda8ecf8a91d3516c6ffa48f48753`; and the develop-relative binary
SHA-256 is `b6975342bb29cb99fec7eef30683b4438ba537083eb7c377205581c7d433bc18`.
Candidate mismatches and develop-only path mismatches were both zero.

No immutable commit was amended. The `T6-R1` review budget is consumed and its
final focused rechecks pass. One logical `T6-C1` correction budget is consumed;
the three immutable physical follow-ups are the commits listed above.

Focused evidence contains exactly 35 Update Unit cases, 22 Update Integration
cases, and three Update EndToEnd cases, all passing. The exactly three Doctor
EndToEnd focused cases remain unchanged and pass. The contributor catalogue
remains six members with the existing Framework and recovery attribution; no
Doctor vocabulary was added.

The local-only locked restore covered all six projects without source or
download warnings. The pre-integration Release solution build completed with
zero warnings and zero errors. Managed Unit `1832/1832`, Integration `988/988`,
and EndToEnd `181/181` passed. The worktree-built public executable reports
`0.0.0-dev`, with SHA-256
`4450c4552ac44a4e463db6c9adad89a39d803da47801801019ee08c02045bd61` and marker
SHA-256 `fe4d33c8c2c76a67725ea7d54dafb79c485bd2819a317d7235a6910157ef76f4`.

Native root, Integration, and EndToEnd publishes each had literal exit `0` and
no warning or error lines. Their full before/after path, mode, and byte
manifests were identical: root has four entries with aggregate
`04dedf5f091a8406efa322b02ff661cd331d82c459f9d90529336357a26be168`,
Integration has 86 entries with aggregate
`43cdd8a93c415941f11760ac66c4605791adc46ba41be3c7314f63d3c20c70da`, and
EndToEnd has five entries with aggregate
`c08951d3030b9ac1489a18253449b6cb21c8adc9871308a4975c1278e7184ff1`.
Native artifact SHA-256 values are root
`51a7d0c287e4df7b4d1b638bd119881065720d027c6f56ebb15b10f469a78518`,
Integration `d182adf2fb5cfbed71aedecb52ac408b11175d979787b6fbd77edfc7c96477ca`,
and EndToEnd
`d5d46f01adbe1895b1778ed7ce6dfa8064e24c5626a72060f9a7980aadc51beb`.
All three native outputs are ELF64 x86-64 PIE artifacts. The root CLI and its
version marker report `0.0.0-dev`.
Managed-on-native EndToEnd passed `181/181`, Native Integration passed
`988/988`, and Native EndToEnd passed `181/181`; all failures and skips were
zero. A globally resolvable executable was identified but never invoked. All
evidence uses only artifacts built in Task 6 snapshots.

The fresh post-integration main Release build completed with zero warnings and
zero errors. Its freshly built managed EndToEnd app selected the sole
`PublishedUpdateProcessTests` class, which contains exactly three public facts;
total, discovered, executed, and passed were `3`, with failed and skipped both
`0`. The same-workspace development executable is
`artifacts/publish/open-forge-dev/Release/open-forge-dev`, with SHA-256
`4450c4552ac44a4e463db6c9adad89a39d803da47801801019ee08c02045bd61`; its
`artifacts/publish/open-forge-dev/Release/open-forge-dev.version` marker has
SHA-256 `fe4d33c8c2c76a67725ea7d54dafb79c485bd2819a317d7235a6910157ef76f4` and
reports version `0.0.0-dev`. The global PATH CLI was not used as acceptance
evidence.

### Residual Risks

Native acceptance covers the supported `linux-x64` target and does not extend
to other RIDs. The accepted cooperating-process boundary remains in force; no
hostile same-user guarantee is claimed. The replacement CLI remains
non-shipping until a later authorized release boundary.

### Simplified Flow Trial Observation

Command-private Gray/Red work and isolated mechanical-gate parallelism were
beneficial. Cold handoff stalls, split semantic Red fan-out, receipt
aggregation, checkpoint bypass, yielded-process misclassification, and
self-matching wait loops added work. Fresh review found one real production
safety defect plus missing apply proof; two correction rechecks caught
classification and evidence-fidelity gaps; the first full gate found a stale
Install oracle. These quality-positive checks required correction cycles. No
performance measurement or numeric acceleration estimate is recorded.

Next exact boundary: after this Task 6 completion, do not activate Task 17 or
any later task. Report the complete remaining task list — Task 17 “Extension
Update”, Task 18 “Extension Remove”, Task 19 “Repair”, Task 20 “Cleanup”, Task
10 “CLI Command Surface Audit”, conditional Task 21 “CLI Command Surface
Remediation”, Task 13 “Native linux-x64 CI and Reproducible Artifacts”, and
Task 22 “Final Documentation, Acceptance, and Release” — then stop. Task 17
remains `QUEUED`; existing queue order and completion-grace controls remain
unchanged.

## Expected Outcome

Root `update` reconciles only lifecycle-managed Framework files and regions from
their recorded identity to the accepted embedded payload while preserving
workspace-owned content and producing one verified external recovery bundle
covering every existing target it replaces or deletes.

## Architecture

- Share payload identity, managed-file facts, lifecycle parsing, and ownership
  comparison with Install under the nearest root lifecycle capability.
- Use each target's concrete path, nullable `sourceAssetPath`, region/generated
  identity, and baseline fingerprint. Accept structurally valid historical source
  paths so current inventory absence can prove retirement; validate newly
  published non-null paths against the current inventory.
- Keep update selection, drift classification, conflict policy, plan, ordering,
  findings, result, and renderers local.
- Separate observed current bytes, expected prior managed identity, intended new
  bytes, user divergence, and application receipts.

## Evidence

Cover current/no-op, version change, managed drift, user-owned surrounding text,
missing managed file, unexpected replacement, lifecycle missing/malformed/unknown,
historical retired source assets, generated-region `null`, legacy ordinary files,
dry run, lock race, bundle preparation and retention on
partial failure/cancellation, generated navigation, idempotence, process,
packaged payload, and AOT.

## Stop Conditions

Stop before overwriting unrecognized divergence, guessing prior identity,
migrating legacy state, applying without recorded lifecycle, or treating embedded
payload version alone as proof of current workspace bytes.
