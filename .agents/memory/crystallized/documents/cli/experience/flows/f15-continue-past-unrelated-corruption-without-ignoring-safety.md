---
open-forge:
  description: "Continue past unrelated corruption without ignoring safety"
  tags: [Memory, Document, CLI, UserFlow, Evergreen]
---

# F15: Continue past unrelated corruption without ignoring safety

**Who:** A maintainer whose project contains old or damaged files unrelated to the current action.

**Execution:** See the separate [run record](../../../../../archived/beta-preparation/cli-experience-run.md). Inclusion records a reviewed user outcome, not a passing implementation.

## Review adjustment

Use unrelated malformed routed Markdown in an untouched branch, not an inert .log, to exercise real tolerance. Finish independently safe selected effects and report any genuinely unavailable related checks. Never infer unknown ownership from a damaged record.

This desired behavior is pending flow validation before test implementation. A current CLI disagreement is recorded as evidence, not copied into the expectation.

## Starting point

PACKAGES with valid explicit inputs and an unrelated malformed .agents/patterns/old-note.md.

## Flow

Run the steps on the actual resulting state. A linked scenario supplies a verification boundary, not permission to reset the workspace between steps.

| Step | Action | Scenario | State passed forward |
| ---: | --- | --- | --- |
| 1 | Run the otherwise valid explicit Extension install. | [X12](../scenarios/experience.md#x12) | The unrelated corrupt file does not become a universal admission gate and remains untouched. |
| 2 | Fork with an absent, unreadable or malformed generated ownership lock instead. | [X13](../scenarios/experience.md#x13) | No ownership is inferred; unavailable ownership facts are reported without hiding known useful facts. |
| 3 | Fork with a genuinely unreadable required package source or required recovery boundary. | [X12](../scenarios/experience.md#x12) | This necessary missing fact still stops the unsafe operation. |

## Alternatives and recovery

### Read-only ownership inspection

Distinguish unavailable claims from known empty registrations.

Scenarios: [X13](../scenarios/experience.md#x13).

### Explicit attach with missing or invalid ownership

A missing lock is known empty and permits an otherwise safe Attach. Invalid or unavailable required ownership blocks before effects; preserve its bytes and report the correction needed.

Scenarios: [C26-12](../scenarios/commands/c26-library-attach.md#c26-12).

## Final result

Tolerance is based on relevance and authority, not on treating every damaged file as harmless or fatal.

## Verification

Observe the resulting files and links independently of printed output. Record actions, stdout, stderr, exit, preserved bytes, and separate outcome/state/communication verdicts in a run record. Carry real state between steps; fork only at an explicit alternative.
