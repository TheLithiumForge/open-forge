---
open-forge:
  description: Accepted beta correction execution, wave ownership and verification state
  tags: [Memory, Working, CLI, Plan, Contextual, Active]
---

# Beta correction execution

## Authority and horizon

The maintainer approved implementation of the reviewed packet on 2026-09-21.
Execute D1–D6, E1–E2 and N1–N3. E3–E9 remain forecast work until their exact
producer mappings and expected output are defined after the representative gate.
Preserve default Index scope; automatic Skill-resource traversal is not selected.
Use the Development recipe through the existing Use Workflow Skill.

## Applicability

This local CLI changes developer-owned Markdown workspaces. This correction
changes documentation, retained diagnostic facts and human guidance. It does not
change filesystem operations, ownership, recovery, status/exit policy or JSON
schema. Source edits are reversible through reviewed Git diffs; existing local
changes remain protected. Use the BCL and existing fact/projection contracts;
exceptional machinery: none. No mock filesystem or new dependency.

Pure mapping tests prove identifier and wording selection. Real module tests
prove manifest reads and retained paths. One published journey proves the
advertised Index action repairs the diagnosed catalogue. The integrated public
output and source-generation boundary triggers complete managed and Windows
Native AOT verification after mutation waves drain. Prepared offline dependencies
are used; no hosted pipeline, push, merge or publication is selected.

## Wave and gates

- Wave 1: D1–D6, E1 and N1 with exact disjoint ownership from the linked packets.
  Their specified meanings, E1 internal fact and N1 expected identity are accepted.
  Existing JSON projection models exclude the internal Extension read result.
- Gate 1: drain, inspect all diffs, run focused E1/N1 tests and review prose.
- Wave 2: E2 after E1 acceptance, N2 after N1 and argument-advice freeze.
- Wave 3: N3 after N2; coordinator owns command contracts and exact snapshots.
- Final: focused regression review, format/analyzers, managed and native gates.

One direct coordinator owns integration and Git. Local dispatch records bind
worker/run IDs, input hashes, exact file allowlists and unique receipts. Runtime
identifiers and raw machine output stay outside distributable task records.
Review budget: one coherent documentation review and one focused code review of
fact preservation, action targeting and serialization; one grouped correction
pass per review, followed by affected rechecks.

## Current checkpoint

All implementation waves are drained. D1–D6, E1–E2 and N1–N3 have passed
focused acceptance. Documentation review found no actionable defects. The
focused code review found R1, corrected below; its bounded independent recheck
confirmed resolution with no remaining defect. Final managed/native qualification
passed; all approved slices are accepted.

| Boundary                                   | Observed result                                                      |
| ------------------------------------------ | -------------------------------------------------------------------- |
| E1 manifest reader and source wrappers     | 18 integration cases passed, including real Windows sharing denial   |
| N1 expected route identity                 | 11 pure mapping cases passed                                         |
| E2/N2 mapping and presentation             | 52 unit cases, 14 application integration cases passed before R1     |
| Platform expectation controls              | 3 unit cases passed                                                  |
| Accepted snapshots and OutputText identity | 6 cases passed                                                       |
| N3 published Doctor recovery journey       | 7 process cases passed                                               |
| R1 installed-only correction               | Clean Release build; all 17 Extension List presentation cases passed |
| .NET whitespace and analyzers after R1     | Passed                                                               |
| Final six execution modes                  | Passed: all six modes                                                |

The new published journey installs Planning, adds a recipe, observes default
Index leaving its detached catalogue unchanged, then follows the exact validated
Doctor target through a process argument array. Doctor returns zero with no
findings after the targeted Index. Recipe and native Skill bytes stay unchanged.

Exactly 16 accepted source-invalid/source-unreadable main snapshots changed.
All eight JSON frames differ only in headline, finding message, unavailable
explanation and Next. Raw diagnostic captures and unrelated snapshots did not
change. A supplemental real Linux sharing probe passed all eight detail/format
runs and preserved manifest bytes. Its general I/O wording is an explicit,
fixture-specific expected-platform distinction; received output is never
rewritten. This is not full Linux or macOS qualification.

## Integration and review dispositions

- The first E2 build exposed a Framework type crossing into Rendering. The
  coordinator introduced an Operations-owned failure detail with an explicit
  five-kind projection, preserving the five-project boundary. No project
  reference, public schema or filesystem operation changed.
- The coordinator corrected public test signatures that exposed internal enum
  types. A subsequent build reported seven assertion-analyzer violations; those
  assertions were corrected. The next build passed without warnings or errors.
- Doctor construction retains the cohesive comparison object. Mixed-catalogue
  Next selection prefers copyable Index advice only within the accepted targeted
  navigation cases; unrelated first-finding behavior stays unchanged.
- Typed Extension source failures retain their precise manual recovery action
  when installed-owner inspection also offers an action. No-detail behavior is
  unchanged.
- R1 (P2), at `ExtensionListReportSelector.Headline`: a typed source failure
  could describe the unselected Available section for `--installed`. Accepted
  and corrected by guarding the headline override on `Selection.Available`.
  Two pure cases cover invalid and unavailable states across all four detail
  levels, text and JSON; precise findings and manual advice remain. The earliest
  invalidated boundary was Rendering/output qualification, so the native build
  was repeated before the final complete test gate. The bounded independent
  recheck confirmed R1 resolved; no further correction was requested.
- The independent documentation review found no actionable defects and resolved
  all supplied relative links and 20 heading links. The coordinator synchronized
  the two immediate parent Entries descriptions afterward.

## Integrated qualification

The final Windows build passed without warnings or errors. The managed and
Native AOT suites use the same frozen source and validated artifact manifest.
Tests run with collection parallelism; exclusions are reported separately.

| Execution mode                   | Passed | Platform exclusions | State  |
| -------------------------------- | ------ | ------------------- | ------ |
| Managed unit                     | 3311   | 0                   | Passed |
| Managed integration              | 2347   | 17                  | Passed |
| Managed published CLI            | 246    | 0                   | Passed |
| Native integration               | 2347   | 17                  | Passed |
| Native published CLI             | 246    | 0                   | Passed |
| Managed tests against native CLI | 246    | 0                   | Passed |

The six reports contain zero failed, pending or other results. Artifact hashes
and source identity validated, and the delivery manifest records `tested: true`.
The 17 exclusions in each integration mode are the existing platform policy,
not newly skipped regressions. The managed/native gate covers 8,743 passing
executions across modes, not 8,743 distinct test cases.

The gate ran before staging and committing the same product-file bytes. The
source manifest intentionally binds the pre-commit working state; it is not
retagged as a post-commit build. Rebuild after a Git-state change before using
that manifest for a later packaging or publication gate. Machine-specific
receipts, runtime identifiers and binary artifacts remain ignored local evidence.

## Remaining scope

E3–E9 require current producer inventories and frozen per-situation expectations
before dispatch. Historical ownership-transfer and user-edited-file upgrade
requirements remain unqualified. Automatic Skill-resource traversal remains a
product decision; the accepted fix makes the existing targeted action accurate.
Other beta backlog items are not activated by this execution. No hosted pipeline,
push, merge, release or publication has been performed.

## Reproducing the checks

Run from the repository worktree with its prepared dependencies:

```powershell
npm run build -- --no-restore
dotnet artifacts/bin/OpenForge.Cli.Core.UnitTests/release/OpenForge.Cli.Core.UnitTests.dll --filter-class '*ExtensionListBindingAndPresentationTests' '*DoctorWorkspaceRouteMappingTests' '*DoctorIndexActionTests' '*DoctorTargetedIndexAdviceTests' '*PlatformSnapshotComparerTests' --minimum-expected-tests 1 --parallel collections --no-ansi --progress off
dotnet artifacts/bin/OpenForge.Cli.IntegrationTests/release/OpenForge.Cli.IntegrationTests.dll --filter-class '*ExtensionManifestFileReaderIntegrationTests' '*ExtensionSourceIntegrationTests' '*ExtensionListApplicationIntegrationTests' '*ExtensionListBeforeOutputSnapshotTests' '*OutputTextIdentityTests' --minimum-expected-tests 1 --parallel collections --no-ansi --progress off
dotnet artifacts/bin/OpenForge.Cli.EndToEndTests/release/OpenForge.Cli.EndToEndTests.dll --filter-class '*PublishedDoctorGeneratedNavigationProcessTests' --minimum-expected-tests 1 --parallel collections --no-ansi --progress off
npm run check:dotnet
npm run build:native -- --no-restore
npm run test:built
```

Native compilation requires the existing Windows C++ toolchain and its discovery
tool on the process PATH. Snapshot verification normally runs without update
variables. The one accepted baseline update selected only SourceBoundary, ran
serially, restored the environment afterward and checked the exact16-file allowlist.
Supplemental Linux evidence is not a claim of hosted or macOS qualification.
