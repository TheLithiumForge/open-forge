---
open-forge:
  description: Correct the seven validated CLI audit findings within accepted behavior, test tiers, and local callable ownership
  tags: [Memory, Working, Contextual, Complete, CLI, Task, Remediation, Architecture, Refactoring, Testing]
---

# Task 21: CLI Command Surface Remediation

## M6 Integration Acceptance

Task 21 is complete at phase 4/4, milestone 6/6. Accepted feature `6173fb5b`
was squash-integrated into local develop as `75f6ff49`, from `8463a2e2`.
Both have exact tree `0b7ed8e619d7a4208c805c04e8a038860f88cfbd`. The final
freeze accounts for 95 changed paths, including sixteen additions and one
deletion (94 displayed changes with rename detection). No reviewed source
changed during integration, so the qualified M5 receipts remain applicable.

`artifacts/task21-final/integration-receipt.json` records every accepted path
and source identity. Task 27 reconciled the new ancestor at `12d15251`; its
pre-existing authority tree remained exactly `fa23fa6e`. All older dirty
worktrees and the separate Task 28 work remain preserved. No remote action,
publication or global installation refresh occurred. The earlier M5 and M4
sections below retain their historical evidence and superseded pending states.

## M5 Full Gate Acceptance

Root accepts M5 on the unchanged reviewed source `391ad956` under authority
`c1c1cf1a`. Full managed passes 2,879 Unit, 1,603 Integration and 111 public
cases. Native linux-x64 passes 1,603 Integration and 111 public cases; managed
public tests selecting that same native CLI pass all 111. All 6,418 executions
pass, with zero failures/skips and zero build/publish warnings or errors.

The canonical receipt is
`artifacts/task21-final/after-c2/canonical-summary.json`. Exact discovered and
executed identities, source/configuration and runtime manifests, native ELF
identity, CLI version/help and formatting/static/protected checks qualify the
candidate. All 71 touched C# paths and all 24 unrelated dirty files are accounted.
The earlier interrupted native attempt remains unqualified and preserved.

Task 21 is phase 4/4, milestone 5/6. Only M6 exact final freeze and integration
remain. No remote publication, global installation refresh or six-platform
execution is claimed. The accepted feature candidate may supply Task 27's
preflight and subsequent isolated branch while Task 21 integration is pending;
reconcile exact source trees at each eventual integration boundary.

## M4 Review Acceptance And Final Candidate

Root accepts M4 after the same reviewer's bounded T21-C2 return. Candidate
`391ad956` contains the isolated four-line References assertion; its source hash
is `8fb0c39fbe20d6b0038262f93b5a22aafc8ff90c787e51a4db535771cf430b12`.
All prior assertions and fixture bytes are preserved. Original 28 now proves
the exact serialized incoming source path. Originals 62/69 and all nine helper
invocations have accurate condition and no-write owners. The reviewer verified
the fourteen relevant owner bodies and source hashes; T21-R1-1 is closed.

The accepted corrected semantic record is
`artifacts/task21-c2/final/semantic-claims.json`, SHA-256
`3ebafda35a82f609f5698e918c221acdcaeea24d9f0d7aaf502a54d4b5416589`.
Its packet, mapping dispositions and source archive preserve exact evidence.
Both author and reviewer personally read and fingerprinted the current complete
C# directive trio. T21-R1, T21-C1 and T21-C2 are consumed; no further review is
selected for this unchanged boundary.

Fresh full managed evidence on `391ad956` passes 2,879 Unit, 1,603 Integration
and 111 public cases: 4,593 passes, zero skips/failures and a zero-warning/error
build. Discovery/execution identities and unchanged source/runtime manifests
are qualified under `artifacts/task21-final/after-c2/`. C2's fresh runtime changed
22 prior entries, so root reran full managed evidence rather than reusing C1.
All 71 touched C# paths retain current formatting evidence; static/protected
checks and preservation of all 24 unrelated dirty files pass. Native gates and
integration remain pending; Task 21 is phase 4/4, milestone 4/6.

## Bounded Recheck Correction

The same reviewer's C1 recheck accepted the prior named gaps, Index journey
preservation/default-store checks, and the three ordinary status rows. It found
one remaining missing value assertion: the serialized incoming References
occurrence's `source.path`. Existing mapped owners prove a target basename,
view equality or property order, not that source value. Add one independent
literal assertion to the existing composed incoming-JSON Integration owner;
freeze that file first and preserve every existing assertion and fixture.

The other remaining defects are receipt-only. Original 62 must link actual
Generic Init omitted-metadata Attention/NeedsAuthoring and dry-run no-effect
owners. Original 69 must link actual authored-template protection and missing-
template resolution. Map their implicit no-write helper calls individually to
the invalid, blocked, incomplete or attention branch actually invoked. Existing
live tests already supply these conditions; no duplicated tests are requested.

Root increases the grouped correction budget from one to two for this concrete
recheck result. `T21-C2` owns only that References assertion and these bounded
receipt corrections, using the same author and reviewer. The fresh holistic
review budget remains one; no production or wider review scope is reopened.
Preserve the C1 packet and failed recheck, create a separate corrected packet,
and recheck only these remaining facts. The native run was stopped before
qualification; partial artifacts are unqualified and preserved. Managed C1
results remain evidence for their exact candidate, not the forthcoming change.

## C1 Candidate And Full Managed Evidence

The grouped tests-only correction is committed as `05725537`, tree `1a232ccc`.
Root verified all 2,449 compiled source/configuration identities, 320 runtime
files and the exact bytes/modes of all seventeen changed test files before
staging. Production is unchanged; no formerly untracked source remains in this
correction. The final packet is `artifacts/task21-c1/final/packet.md`, with
claim-level reconciliation in its adjacent `semantic-claims.json`.

Focused evidence passes 468 Unit, 541 Integration and three Index public cases,
with zero skips and a zero-warning/error build. The final map covers 96 original
methods, 1,020 assertion occurrences and 319 semantic groups. Earlier mapping
attempts remain preserved and superseded; counts and owner hashes alone do not
prove that each owner exercises the required condition and fact.

Root additionally accepted three literal Complete/Incomplete/Blocked rows in
existing `PipelineTests.HumanOutputAndCompletionFollowStatusPolicy`. Together
with its original four rows these prove all seven ordinary status mappings.
The earlier all-seven claim was unsupported. Terminal Context help is proved
through its actual composed help branch, which bypasses ordinary result output.
The shared test file was frozen before modification; its behavioral test body
was retained. This narrow amendment remains within T21-C1.

All three retained Index journey bodies are byte-identical to their freeze.
Strengthened lower evidence was frozen before pruning its two displaced public
methods. Full discovery and execution now contain exactly three journeys for
all 28 commands: 84 command cases, 26 Shell cases and one artifact case.

The full managed gate passes 2,879 Unit, 1,603 Integration and 111 public cases:
4,593 passes, zero skips/failures and a zero-warning/error build. Exact case
identities, source/configuration hashes and runtime hashes qualify this result
under `artifacts/task21-final/after-c1/`. All 71 touched C# paths have current
informational formatting evidence; root static and protected-path checks pass.
The same reviewer is rechecking T21-R1-1 and the bounded amendments. M4 remains
open until that return is accepted; native gates and integration remain pending.

## T21-R1 Review And Grouped Correction

Fresh reviewer `cli_remediation_review` assessed immutable `037a0c15`, tree
`61336e57`, and returned one accepted P2 finding, **T21-R1-1: assertion
preservation and evidence integrity**. No material production defect was found
in the scoped corrections or refactors. The reviewer personally read and
fingerprinted the complete current C# directive trio.

The Purple receipt grouped original assertions under method-level owners that
sometimes proved different facts. Three confirmed evidence gaps require focused
coverage: Context help operand/content grammar and section headings; real
expanded-human plain/verbose equality and bounded diagnostics for Route Inspect;
and Route Update failed-verification human output including retained recovery
and its actual residual path. Receipt-only omissions include existing Route
Inspect related-command help evidence, Find blocked-human rendering/streams,
and Route Update invalid-patch binding. Reconcile the original exact cause text
and Route Init invalid/incomplete presentation claims explicitly. These
examples require semantic reconciliation of all unique original claims, helpers
and theory rows; passing owner identities alone are insufficient.

`T21-C1` is the one grouped tests/evidence correction, with production frozen.
Map each claim to actual assertions and conditions. Reuse equivalent existing
owned evidence, add narrow Unit/Integration assertions for gaps, and explicitly
dispose obsolete expectations only when accepted contracts justify that result.
Keep the deterministic replacement for the former polling test. The same
reviewer rechecks this finding and the bounded amendment below; no new holistic
review or open-ended review loop is selected.

The full managed candidate build passed without warnings/errors; all 2,869 Unit
and 1,593 Integration tests passed. Public discovery stopped before execution:
Index retained five inherited public tests, while every other command had three.
The root receipt is `artifacts/task21-final/managed-inventory-failure.json`.
This is an allocation gap outside the initial ten-command list, not a production
execution failure. Root accepts an additional tests-only Index amendment to meet
the standing three-simple-public-journeys requirement.

The exact Index freeze is `artifacts/task21-index-preflight/packet.json`. Retain
its dry-run, apply/no-op and invalid-JSON bodies byte-for-byte. Preserve the two
displaced methods' 19 explicit assertions and five no-write boundaries through
existing Integration owners, strengthened with per-invocation snapshots and
absence checks for the unique workspace's actual default-store lock/recovery
paths. The proposed private `AssertNoPersistentState(CliWorkspace)` and
`AssertAbsent(string)` helpers stay in IndexApplicationIntegrationTests and reuse
owned identity/path derivation. Do not inspect the fixture's separate unused
lock store or claim absence of shared parent directories. Retained published
dry-run evidence still proves its isolated store boundary.

Only PublishedIndexProcessTests and IndexApplicationIntegrationTests expand the
original command allocation scope. Additional lower-tier edits needed for
T21-R1-1 stay with the original claims' command owners and are listed/frozen
before modification. Freeze strengthened lower assertions before pruning Index.
Final public inventory must contain 84 command journeys plus the existing
26 Shell and one artifact cases. Preserve prior receipts and qualify any reuse
against exact sources and runtimes; no full acceptance is inferred yet.

## M3 Candidate And Review Boundary

Source candidate `097e3430` contains the separate 28-path tests-only Purple
commit. Root froze all bytes and modes before staging, including four formerly
untracked files. Production is unchanged by Purple. The candidate follows the
separate behavior and Blue commits recorded below.

Final focused evidence qualifies 382 Unit, 508 Integration and 90 public cases,
with zero failures/skips and a zero-warning/error build. Public evidence is
reused only across identical public source, runtime, publication and configuration
hashes; the final two nullability corrections affect Integration tests alone.
All 146 frozen Red identities pass. All 62 touched C# files have source-bound
informational formatting receipts; root static/protected checks pass and all 24
unrelated dirty files retain their original bytes and modes.

The final packet is `artifacts/task21-focused/final/m3-packet.md`; the exact
candidate and assertion mapping are adjacent. The mapping preserves 96 original
public methods and 1,020 assertion occurrences through 203 executed owners.
Root checked source identities and mapping completeness; semantic sufficiency
is part of the fresh holistic review, not inferred from assertion counts.
The root stage freeze is `artifacts/task21-final/purple-commit-freeze.json`.

Fresh review `T21-R1` examines this immutable source plus its Task authority.
Full managed/native acceptance and integration remain pending. No claim of
six-platform execution or final Task acceptance is made by these focused gates.

## Route Init Convergence Clarification

The new public generic-Init journey initially repeated its creation metadata
against the newly existing target and failed with InvalidMetadata. The accepted
Interface's existing-entrypoint rule, Behavior's metadata rule, planning inspector
and prior `RepeatedApplyIsIdempotentAndSecondRunIsNoOp` Integration case all require
omitting creation metadata on that no-op invocation. Root clarified only the two
contracts' overly broad repeat sentences to agree with their existing specific
rules. No product behavior, parser or result policy changes. M1's generic
preview/apply/no-op journey retains this state-dependent valid invocation.

Root initially compared the failed run with a newer working-file draft and
reversed the cause. The author supplied the exact archived build input and
corrected the diagnosis. Failure interpretation must use the executed source
identity, even when the current file retains the same line number.

## Isolated Behavior And Structural Commit Boundary

The behavior-only candidate `8a52ede1` passes 64 Unit and 90 Integration cases
with no skips and a zero-warning/error build in the isolated validation worktree.
The eleven-path source freeze and exact execution identities are retained under
`artifacts/task21-isolation/`. The prior Red fixtures and assertions are unchanged.
This is an intermediate feature commit; final Task acceptance remains pending.

Its informational formatting check reports only CA1822 on
`RouteInspectSourceProjectionBuilder.ReadAsync`. Root accepted a separate
structural correction: make that stateless class and method static, remove the
resolver's field, and update its receiver. The two directly consuming Integration
test files need exactly three instance-to-static receiver substitutions. Their
arguments, fixtures, assertions and test identities stay byte-identical after
normalizing only that literal substitution. This narrow callable amendment avoids
fake instance state, compatibility wrappers and warning suppression. It consumes
no additional review budget and must pass the final formatting gate.

The fourteen-path Library production refactor is validated separately against
byte-frozen tests. Public journey pruning and lower-tier assertion changes remain
the later isolated Purple commit; no assertion removal belongs in either Blue
commit. The three-commit minimum may therefore contain another coherent Blue
commit for the stateless Route Inspect capability.

Library Blue is committed as `a48f673d`. All 616 selected Unit and 426 Integration
cases pass both before and after the fourteen production changes, with matching
discovered/executed identities and unchanged test bytes. Both builds have zero
warnings/errors. The before/after receipts are under the isolated worktree's
`artifacts/task21-library-blue-baseline/` and `artifacts/task21-library-blue/`;
the exact fourteen-path stage freeze is
`artifacts/task21-isolation/library-blue-commit-freeze.json`. Final combined
formatting, review, public allocation and full managed/native gates remain open.

The stateless Route Inspect amendment is committed as `de1cc387`; all 84
selected Route Inspect Integration cases pass with zero build warnings/errors.
The three test receiver substitutions reverse exactly to frozen test bytes.
Commit `56f1260b` contains only the comparison helper's concrete private List
parameter and array-expression style. All twenty touched production files pass
`--verify-no-changes --severity info`, with source hashes in the isolated
`artifacts/task21-production-format/verify-result.json`. The combined build and
remaining touched-source formatting are still required.

## Task State

- State: Active after Task 10 acceptance and integration `fbcec295`.
- Permanent mapping: Task 21 “CLI Command Surface Remediation” in the
  [project control ledger](../project-control.md).
- Phase and milestone horizon: phase 4/4, milestone 5/6.
- Profile: bounded sequential remediation with explicit callable/Gray and Red
  boundaries, one continuous Astra/high Brilliant Implementer, one fresh
  Astra/high holistic review, grouped correction and final shared evidence.
- Root owns accepted meaning, scope, freezes, Task state and integration.
  The implementer owns the coherent source/test slice and focused evidence.

## Accepted Outcome And Authority

The [Task 10 report](cli-command-surface-audit-report.md) records three behavior
conflicts, two related public-evidence allocation findings and two bounded
callable improvements. The user has explicitly authorized completion of
remaining corrections and refactoring with full internal execution authority.
Root selected exactly T10-R1-1 through T10-R1-3 and T10-R2-1 through T10-R2-4.
No new product choice, support floor, external effect, dependency, compatibility
mechanism or publication is included. Older conditional exact-finding wording
is resolved for this concrete in-scope set; consequential new choices remain
with the maintainer.

Restore existing Update create behavior, exact adjacent overwrite inspection,
and truthful Library recovery-preparation status/residuals. Simplify only the
two named Library callable stages. Retain exactly three simple public command
journeys while moving detailed owned branches to the cheapest proving tier.
Keep distinct Shell and delivered-artifact subjects, including relocated
embedded-payload reachability. Do not disguise a branch matrix as one journey.

## Accepted Commit Boundaries

On 2026-09-09 the maintainer explicitly approved this task and required isolated
refactoring and test changes. Freeze the current callable contracts, observable
behavior and test assertions at Gray before each bounded slice. For a proved
behavior defect, freeze independent Red evidence first and commit the correction
separately from pure refactoring.

Blue changes production structure against frozen behavior and tests. Commit
that production-only change after its focused evidence passes. Purple changes
or removes tests in a separate commit, mapping each unique assertion to retained
or replacement evidence at the appropriate tier. Record why an assertion is
obsolete when removal is justified. Test removal does not authorize behavior
change, and moving assertions must not weaken the accepted contract.

Keep the same continuous author where useful; separate commits and protected
boundaries do not require separate agents. Refreeze all changed, moved, deleted
and formerly untracked files before staging. Final combined evidence follows the
accepted Task scope after the isolated increments are qualified.

## Frozen Scope Capsule

- Base: `8463a2e2`, the accepted audit integration plus prose-only completion
  receipt. Executable source still matches `d5b77fce`. Branch:
  `codex/cli-command-surface-remediation`, same sequential worktree.
- Project: a nonpublic local developer tool that can change consumer files and
  symlinks. Protect against ordinary defects, interruption, malformed input and
  cooperating-process concurrency using existing typed validation, leases,
  recovery and no-follow mechanisms. Do not invent a malicious same-user
  security guarantee, transactional framework or power-loss durability.
- Order: preflight/callable freeze; independent failing evidence for R2-1,
  R1-1 and R2-2; coherent production correction; R1-3/R2-4 local simplification;
  R1-2/R2-3 public evidence allocation; focused qualification, fresh review,
  grouped correction, full managed/native gates and exact integration.
- Behavior ownership: Update's private application adapter passes no recovery
  preparation for ordinary Create; shared FileChangeApplier guards for
  reversible creates and existing-target effects remain unchanged. Route
  Inspect consumes catalogue-owned exact pairs and keeps ID collision separate.
  Library operation/completion carries finite preparation outcomes and actual
  residual identity without fabricating a successful opaque preparation.
- Structural ownership: Inspect comparisons stay under
  `Commands/Library/Inspect/Shared/` and nearest `Models/`; the three-consumer
  mutation observation view belongs in Library Models/Planning and is consumed
  by the existing shared completion stage. Reuse cohesive existing facts where possible. Do not merge
  Inspect policy with List or mutation policy, or hide dependencies in bags.
- Protected meaning: public grammar, JSON schema/field inventory, status/stream
  contracts except the identified incorrect outputs, source/destination mapping,
  permission grants/questions, exact ownership, source preservation, no-follow
  validation, detached-source independence, record-last publication, real
  orphans/route ambiguity, and existing cancellation/recovery semantics.
- Expected source scope: Update Shared/Application; Route Inspect Shared/
  Resolution and only directly affected local model/caller sources; Library
  Attach/Sync/Detach operation and completion, existing Library shared
  Application/Completion/Models, and Inspect-local comparison sources/models.
  Direct mirrored Unit/Integration tests, public test files identified by the
  two evidence findings, their owned fixtures/support only when actually needed.
  Exact path/callable and journey inventories are frozen at M1 before edits.
- Public evidence scope: Context, Find, References, Route List/Inspect, root
  Install and Route Init/Create/Update/Move. Other command journeys stay
  unchanged except an explicitly justified ownership relocation of the named
  neighboring List/Inspect case. Keep all fifteen Library and eighteen
  Extension command journeys. Do not retest upstream libraries or runtimes.
- Protected paths: dependencies, project configuration, packages, CI workflows,
  public documentation, Architecture/contracts and unrelated production/test
  scopes. A contract wording correction requires root inspection before a
  bounded amendment; accepted product meaning may not change implicitly.
- Allowed coordination: this Task, current root plan/parent/checkpoint/ledger,
  generated navigation, and the existing flow observation. The Task 10 report
  is immutable audit evidence; this Task owns correction receipts by stable ID.
- Every C# author and reviewer personally reads the complete current C# trio
  and reports fresh SHA-256 fingerprints. Apply source-locality and contract-
  ownership Patterns, exhaustive enum mapping with undefined-value proof,
  compiler-proven nullable branches, cohesive callables and 200-column source
  bounds throughout. No DI, registry, reflection/string dispatch, compatibility
  machinery, JS/MJS/CJS implementation or warning-silencing null suppression.
- Preflight boundary: the continuous implementer first returns exact callable,
  file and retained-journey/branch-evidence plans without edits. Root freezes
  M1, then author prepares only required Gray surfaces and Red evidence.
  Existing accepted contracts are Gray authority; avoid new stubs or types
  where current callable surfaces already express the corrected behavior.
- Red boundary: prove each behavior defect through the narrowest meaningful
  owned seam; planning-only Update evidence is insufficient. Preserve baseline
  production identity and distinguish failed assertions from setup/teardown or
  tool errors. The pure refactors use existing behavior characterization;
  test-pruning preserves unique assertions before removal. Root freezes M2
  before Green. No blanket test replacement or manufactured null inputs.
- Evidence: build with zero warnings/errors; complete touched-source
  informational formatting, callable/locality/enum/nullability/prohibited-
  pattern/line-length and protected-path gates; focused Unit/Integration and
  retained public selections with discovered/executed identities; fresh review
  on an immutable candidate. At the combined boundary run required full managed
  Unit/Integration/public, supported linux-x64 Native AOT Integration/public,
  and managed-public-on-native gates. All required cases pass with no skips.
  Pin source, toolchain and executable identities; preserve failed attempts.
  Same-worktree executables only; no global refresh or external publication.
- Freeze/accounting: include every added/moved/deleted path by bytes and mode
  before staging, preserve every unrelated dirty/untracked file and earlier
  artifacts, and verify candidate/integration tree identity. No dirty reset,
  restore, clean, stash, rebase or immutable commit amendment.
- Budgets: fresh holistic review maximum one (`T21-R1`); grouped correction
  maximum two (`T21-C1`, `T21-C2`), including bounded affected-finding rechecks;
  council zero. Consumed: `T21-R1` returned accepted finding T21-R1-1;
  `T21-C1` produced tests-only candidate `05725537` for that grouped finding,
  the bounded Index amendment and three missing shared status rows. `T21-C2`
  closed the remaining References value and receipt defects in the same
  reviewer's accepted bounded recheck. Root may explicitly revise a justified internal
  budget within user scope rather than silently exceeding it.
- Current boundary: M5 is accepted at source candidate `391ad956`; review and
  all 6,418 managed/native executions pass. Behavior, Blue and Purple changes
  are isolated. Root owns M6 exact final freeze and integration. Task 27 may
  begin from this accepted feature source on its own branch.

## Milestones

| Phase | Boundary       | Milestones                                                            |
| ----- | -------------- | --------------------------------------------------------------------- |
| 1/4   | Preflight      | M1 exact scope, callable/Gray authority and evidence plan frozen      |
| 2/4   | Implementation | M2 qualified Red frozen; M3 corrected production and focused evidence |
| 3/4   | Review         | M4 fresh holistic review and any grouped correction accepted          |
| 4/4   | Acceptance     | M5 full managed/native gates; M6 exact final freeze and integration   |

## Finding Closure Register

| Finding  | Owned correction                                                                | State                                  |
| -------- | ------------------------------------------------------------------------------- | -------------------------------------- |
| T10-R2-1 | Update ordinary-create recovery forwarding and actual application evidence      | Implemented; focused evidence passed   |
| T10-R1-1 | Route Inspect exact-pair observation with independent collision facts           | Implemented; focused evidence passed   |
| T10-R2-2 | Library finite preparation failure and residual truth                           | Implemented; focused evidence passed   |
| T10-R1-3 | Inspect-local cohesive comparison stage                                         | Implemented; focused evidence passed   |
| T10-R2-4 | Shared Library completion observation view                                      | Implemented; focused evidence passed   |
| T10-R1-2 | First-cohort public journey allocation with unique lower-tier assertions        | Reviewed; full gates passed |
| T10-R2-3 | Mutation-cohort public journey allocation with separate artifact/Shell subjects | Reviewed; full gates passed |

## M1 Accepted Preflight

Root accepted the continuous Astra/high implementer's preflight and refinement
on 2026-09-09. The executable baseline remains `d5b77fce`; Task authority is
`bc870d5b`. Ignored `artifacts/task21-preflight/path-plan.json` identifies exact
planned existing, added, renamed and conditionally affected paths;
`baseline-source.json` freezes source/configuration identities before edits.
Conditional neighbors may change only for an actual dependent caller or unique
assertion. Every final path is independently refrozen and accounted.

The author personally read the complete current C# trio. SHA-256 fingerprints:

- `_csharp.md`: `31045ebcb02d5bfeee8ba9f3112d307b1f72a2186cda618fbf7d22e7d1d90b53`
- `design.md`: `76aa8fc7aaaa79d9535998f5557150f3754e3d80520a7a06864b61659373c1a9`
- `style.md`: `c3fa9d31575e77fedb103ca397f0ccf10ab7236e6edbef1658c7fe36138457cb`

### Callable And Behavior Decisions

Update keeps both existing `ApplyAsync` signatures. Its private adapter sends
null preparation only for ordinary Create; existing shared application guards
remain authoritative. Route Inspect keeps its `ReadAsync` signature and uses
catalogue-owned exact adjacent pairs. Automatic-ID collisions remain separate;
actual orphans, unsafe paths and unresolved selection retain existing policy.

Library adds only nullable `RecoveryPreparationOutcome` of existing type
`RecoveryBundlePreparationResult` to `LibraryExecutionEvidence` for Gray.
Keep `RecoveryPreparation` for an actual successful opaque receipt. No new
delegate, execution overload or test-only production injection is authorized.
Green carries the original finite preparation outcome, maps Incomplete, Blocked
and Cancelled faithfully, and preserves any reported residual path. Failed
preparation has Unknown recovery certainty: an expected residual path does not
prove positive retention. Classify preparation refusal before generic
post-effect failure. Application is NotStarted for Incomplete/Blocked and
Interrupted for Cancelled; verification and record publication stay NotStarted.
Use RecoveryUnavailable for Incomplete/Blocked and Interrupted for Cancelled.
Successful cleanup and post-effect precedence remain unchanged. Undefined enum
values must fail explicitly in the finite mapping helper.

For T10-R2-4, add `ILibraryMutationObservation` under Library Models/Planning.
Its read-only Record, nullable Source, Mappings and Ownership members expose
existing facts. Attach, Sync and Detach planning inputs implement it; Detach's
Source is explicitly absent without reading the source. Shared completion
`Projection` takes observation, libraryId, plan state and sourceIndependent.
All three completions and three request binders are accounted direct callers.
No downstream completion dependency, observation wrappers or copy factories.

For T10-R1-3, add Inspect-local `LibraryInspectComparisonReader` under
Shared/Comparison, constructed with resolver and workspace. `Read` accepts
selected record, nullable inventory and cancellation; its adjacent Models
result carries RegisteredPaths, EligiblePaths, Comparisons and Findings.
`RegisteredPaths` accepts the selected record for unavailable-source handling.
Derive eligibility/completeness from inventory facts. The operation retains
source-boundary findings and their order. No List or mutation policy is shared.
These refactor signatures are design authority; do not introduce unused stubs
before Green merely to create a Gray diff.

### Independent Red Boundary

- Update: two real application cases, new payload plus lifecycle replacement
  and forced missing-target restoration mixed with replacement. Assert exact
  final bytes, preserved unrelated content, lifecycle verification, ordinary
  creates excluded from protected recovery targets, cleanup and convergence.
- Route Inspect: replace contradictory overwrite expectations with actual
  catalogue fixtures for exact leaf base, adjacent overwrite and colliding
  entrypoint. Assert exact pairing, separate collision facts and unchanged
  workspace. Rename the ambiguity test to the exact-pair subject; keep real
  orphan, unresolved-ID and structural-ambiguity controls.
- Library: eighteen completion rows cover three commands, three refused finite
  outcomes and residual absence/presence. Assert semantic status, finding,
  unstarted effects/verification/publication and truthful recovery identity.
  Three real operation Integration cases reach Incomplete through the real
  recovery store. A shared test fixture creates an ordinary-file sentinel at
  the uniquely derived recovery bucket with exclusive FileMode.CreateNew.
  Cleanup removes only its own unchanged sentinel; shared parents remain.
  Seed records and links directly, without a preceding successful mutation.
  Assert a real effectful plan, untouched source/destination/record/permissions,
  unchanged sentinel and no invented residual. No Framework implementation or
  test seam changes are needed. Preserve failed attempts and qualify assertion
  failures separately from build, fixture, runtime or teardown failures.

### Public Journey And Assertion Allocation

The retained allocation is thirty simple journeys across ten commands:

| Command       | Retained journeys                                                                                          |
| ------------- | ---------------------------------------------------------------------------------------------------------- |
| Context       | Ordered startup human; explicit additions/link expansion JSON; missing fragment incomplete JSON            |
| Find          | Expanded human inventory; compact tag/heading exact match; JSON content projection of that match           |
| References    | Default both human; outgoing JSON; filtered incoming JSON                                                  |
| Route List    | Default structured read-only; exact path depth zero compact; invalid depth JSON refusal                    |
| Route Inspect | Complete paired source JSON; exact-path collision attention human; redirected unresolved ID refusal        |
| Install       | Ordinary JSON dry run; automatic apply and verified no-op; redirected human write refusal                  |
| Route Init    | Generic dry run/apply/no-op; Framework dry run/apply/no-op; Framework without trusted installation refusal |
| Route Create  | JSON dry run; exact target/parent apply and no-op; invalid metadata refusal                                |
| Route Update  | JSON dry run; exact base/navigation apply and no-op; ambiguous target refusal                              |
| Route Move    | JSON dry run; leaf/overwrite/reference move consuming old identity; occupied destination refusal           |

A repeat proving convergence belongs to its single mutation journey. Detailed
validation, schema, status, escaping, provenance and recovery assertions belong
to existing command-local binding, presentation, planning or operation tests.
Compare each removed assertion against its concrete lower-tier owner; add any
missing independent assertion before pruning. The final receipt records actual
assertion dispositions, not filename-only coverage claims. Route Update already
has deterministic post-write failure Integration evidence, so remove its public
polling monitor without introducing another test seam. Remove Route Init's
neighbor observation case only after retained List/Inspect coverage proves it.

Keep separate Shell subjects for version, terminal conflicts, parser integration,
delimiter/terminator forwarding, cancellation and Move's extra positional input.
Keep relocated embedded-payload reachability, dry run and no-write/no-lock proof
as delivered-artifact evidence, without its detailed command-schema matrix.
Owned published fixtures may be trimmed only when their consumers disappear.
All fifteen Library and eighteen Extension public journeys stay unchanged.

M1 completes preflight only. M2 requires compiled, qualified Red before root
releases Green. Review/correction budgets are unconsumed.

## M2 Qualified Red Acceptance

Root accepted the final formatted Gray/Red boundary on 2026-09-09. Canonical
owner evidence is `artifacts/task21-red/formatted/qualification.json`; root's
independent source, runtime, format-input and raw-failure verification is
`artifacts/task21-preflight/m2-acceptance.json`. Both earlier compile attempts,
initial formatting diagnostics and the first raw Red execution remain retained.
Test-authoring compile errors are not Red evidence.

The final warning-free build and twelve-file informational formatting gate pass.
All live touched C# lines are at most 195 columns. Root independently verified
2,492 frozen source/input files and 235 runtime files against current bytes,
including discovery/execution correspondence. Managed Unit executes 57 cases:
39 existing controls pass and eighteen preparation-outcome rows fail exactly
on Failed versus Incomplete, Blocked or Interrupted. Integration executes 89:
79 controls pass and ten intended failures comprise Update two, Library three
and Route Inspect five. All 146 cases are accounted; zero skips, pending,
other, fixture, runtime or teardown failures. Assertions after each first
failure remain Green oracles and are not claimed executed by Red.

The three Library operation cases reach a complete effectful plan and pass
workspace/sentinel equality before the incorrect status assertion. Exclusive
file creation and owned unchanged-sentinel disposal preserve shared recovery
parents. Existing reversible-create, source/link safety, cancellation, orphan,
route ambiguity and collision controls pass. No injection seam or Framework
change was needed. The existing Safety test's adjacent-pair collision assertion
was another instance of T10-R1-1; root accepted its bounded expectation correction
from the inventoried conditional neighbors. Two equivalent flag assertions in
that touched file and static theory declaration satisfy informational formatting.

The exact thirteen-path source delta contains one production property, seven
modified existing paths in total, five additions and one deletion. All twelve
live files and the deleted predecessor are frozen. The deleted ambiguity test
is replaced by the exact-pair subject; every formerly untracked file is accounted.
Production matches the preflight baseline after removing the one authorized
Gray property. Core assembly SHA-256 is
`77f8d8dbe00be1cc505e366778e507da64dd30f165db7df828130c05903a1998`.

Root now releases the same continuous implementer for Green under M1's seven-
finding scope, then the two local simplifications and public evidence allocation.
Frozen Red assertions and fixtures remain unchanged. Additional focused tests
for new finite mappings and refactor characterization may be added in their
planned owners without weakening or replacing Red. A correction to a frozen
oracle requires a concrete source/contract conflict returned to root first.
The preparation-status mapping may be internal to the existing completion
capability for direct named/undefined-enum proof; no alternate runtime dispatch
or execution injection surface is authorized. Review and correction budgets
remain unconsumed. Task 27's broader assessment remains separate and downstream.
