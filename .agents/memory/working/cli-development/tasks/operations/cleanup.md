---
open-forge:
  description: Implement lease-validated cleanup of recognized recovery bundles and drafts
  tags: [Memory, Working, CLI, Task, Cleanup, Mutation, Contextual]
---

# Task 20: Cleanup

## Task State

- State: `COMPLETE`. Cumulative Gray and Red
  acceptance is recorded through immutable commits
  `0263ae1e6f63b85b89457c35c28aee29eef1afb3`, tree
  `03f0f7c05f7b2d6b935313fd64de71169f7be7e7`, and
  `e2b7fdc0ef49adaa9fc101e5c9c4e4a603b9807a`, tree
  `7922db08761f53c681937144f94ecef9d886679a`. Green and focused verification
  and execution gates are frozen below. The single grouped correction resolved
  both review findings and the omitted owner line-length gate; fresh gates and
  targeted revalidation passed. Final owner acceptance is recorded below.
- Current position: phase 5 of 5, completed milestone 8 of 8. Task 19 is
  accepted and integrated. Cleanup is root-accepted and squash-integrated at
  `148d378da376d196de183d9564261658164d9d23`, exact candidate tree
  `ab7e488192b435fdefa0b8d30bf1dc853a6b2327`. Queue control remains with
  the Overseer.
- Immutable preparation base: branch `codex/cleanup-implementation`, commit
  `76e8e5f1f58e60a9de159318d10e7b3e9f8fc9c9`, tree
  `ed698b992b68f76b037b4567d154eadbc013239b`.
- Central project-control, plan, and checkpoint records now reflect accepted
  Cleanup integration and Task 23 activation. The released Green and owner
  acceptance snapshots below retain their original evidence boundaries.
- Permanent mapping: Task 20 “Cleanup” in the
  [project control ledger](../../project-control.md).
- Parent: [Operational Commands](_operations.md).
- Contracts: [Interface](../../../../crystallized/documents/cli/contracts/cleanup/interface.md) and [Behavior](../../../../crystallized/documents/cli/contracts/cleanup/behavior.md).

## Outcome And Profile

The selected profile is streamlined assured. The accepted packet provides one
command-private Gray boundary, three disjoint Red evidence boundaries, one
coherent implementation and focused-verification lane, one fresh whole-task
review, at most one grouped correction, and acceptance. Cleanup remains one
direct root operation with no child command, selector, wizard, prompt,
confirmation, or activity classifier.

The result is a complete, typed, deterministic catalogue and deletion plan for
the selected workspace's positively recognized schema-v1 recovery finals and
ordinary exact-name drafts. Deletion is bounded by the held same-workspace
lease, the accepted neutral deletion-validation session, immediate candidate
revalidation, and absence verification. Unknown, malformed, mismatched,
differently keyed, non-ordinary, unavailable, and unsafe items remain
preserved.

## Released Green Baseline And Execution Capsule

The Overseer released Cleanup Green after Task 19 acceptance. Task 19 is
integrated at `11e7a5ed89a8cfef244b87a846a9755f5f37162c`, tree
`b2bd951fde4e3afdad87f64a692597e3d167f609`. Accepted main
`8d334f995ef432908209363fee4b34d200a084a3`, tree
`8b5469e93d89161ac67f581242f6135faee6af93`, adds only the accepted closeout
and queue records. It was merged into the clean Cleanup lane at
`0182eef2542924987fd4679e9c8b3cef883283df`, tree
`5a828f07c7ed5af5eb11601314aeaa5761154344`.

The merge contains exactly accepted main plus the 31 unchanged Cleanup C# files
and this task's preserved detailed capsule. Central and peer task Markdown
conflicts use accepted main. There were no C# conflicts. No original Gray or Red
byte changed. The accepted five-phase enumeration is corrected above without
changing phase 4/5, completed milestone 3/8, or the eight-milestone horizon.

The Task Mastermind controls this capsule and acceptance. One continuous
Brilliant Implementer, `/root/faraday_cleanup/cleanup_brilliant`, uses GPT-6
Astra with high reasoning for the released implementation, local refactoring,
focused verification, and any grouped correction. Its completed read-only
preload personally read all three current C# sources and reported:

- `_csharp.md`: `31045ebcb02d5bfeee8ba9f3112d307b1f72a2186cda618fbf7d22e7d1d90b53`.
- `design.md`: `76aa8fc7aaaa79d9535998f5557150f3754e3d80520a7a06864b61659373c1a9`.
- `style.md`: `c3fa9d31575e77fedb103ca397f0ccf10ab7236e6edbef1658c7fe36138457cb`.

The C# trio, Cleanup contracts, shared operation contract, mutation/recovery
design, recovery implementation, physical identity, and lease implementation
are unchanged by this merge. Newly integrated Repair and Extension Remove
supply their trusted typed attribution and expected-state facts through the
existing recovery input contract. Their actual callables and root composition
are revalidated before source mutation. Existing single-candidate guard policy
and callers remain protected.

The released Green neighborhood includes command-private Cleanup behavior,
command-local `Shared/Planning`, `Shared/Application`, `Shared/Rendering`, and
concrete source-generated JSON metadata under the command's presentation models;
explicit standalone/root composition; and the accepted neutral recovery
deletion-validation session at the existing recovery capability. Supplemental
real-filesystem session evidence may be added in separately frozen integration
test files at the matching scope. The original three Red sets remain protected,
subject only to the explicit Gray and conformance exceptions below.
The release adds no authority over peer command implementations, producer
policy, shared public schemas or process mapping, lifecycle or recovery
preparation, generated source, project/package configuration, or suite cleanup.

The proportionality check selects streamlined assurance for irreversible
recovery-artifact deletion and the new public composition boundary. This is a
local developer tool using standard managed BCL APIs with stable directory
mappings and cooperating-process exclusion. The operation preserves ordinary
workspace content and malformed or unsafe artifacts. A verified deleted bundle
has no automatic recovery, so exact identity, held-lease validation, bounded
deletion, and positive absence are required. Hostile same-user namespace attacks
remain outside the accepted threat boundary. Exceptional machinery: none.

Unit evidence proves finite contracts and projections. Real-filesystem
Integration evidence proves catalogue, lease, session interleavings, monotonic
effects, and absence. Exactly three public journeys prove delivered composition.
The public command, serialization, and deletion-safety changes trigger complete
managed Unit/Integration/EndToEnd and supported `linux-x64` Native AOT gates.
Full-delta severity-info formatting covers every affected project, including
inherited Gray/Red source. All gates use only this worktree's fresh artifacts and
record source/configuration identities, commands, selection and execution
counts, failures, skips, warnings, and limits. Cached-only locked restore is
permitted when needed. Network restore, global installation, publication,
remote Git, peer-worktree mutation, and broad filesystem cleanup remain excluded.

The accepted predecessor receipts are managed `1979/1045/190`, Native AOT
`1045/190`, managed EndToEnd against the native root `190`, and focused `119`,
all with zero failures, skips, and warnings. These are predecessor evidence,
not Cleanup acceptance. The read-only formatting inventory at `96ed0aa3`
returned exit zero and empty reports for four project commands supplied with
31 C# paths; its ignored receipt is
`artifacts/task20-format-inventory/receipt.json`. It ran no build or test.
That initial clean-conformance claim is invalidated: later severity-info
verification found inherited diagnostics in the same original files with
unchanged configuration. A controlled local probe returned exit zero and an
empty report for an absolute include while the unchanged known-diagnostic file
produced `IDE0034` with a repository-relative include. This establishes a local
selection limitation, not a broader formatter claim. Current verification uses
repository-relative includes with an explicit intended file inventory. Supplied
file counts alone do not establish analyzer coverage, and this initial
inventory is not accepted conformance evidence.

Budget maxima remain one fresh whole-task review (`T20-R1`), one grouped
correction (`T20-C1`), and zero councils. Consumed IDs: `T20-R1`, completed,
and `T20-C1`, completed. The fresh review
includes behavior, C# conformance, production structure, and test/evidence
quality. No separate Blue, Purple, topic, or writing-review phase is selected.

## Supplemental Red And Bounded Gray Corrections

Supplemental evidence is frozen at
`af15f1b653499461a967ff5d1e9b06d3d0a4ff00`, tree
`ef7cce06ffc3f289af9dc09dd2d27fcb4976c2bf`. Its seven files contain four
unsupported production callables, eleven real-filesystem session Integration
cases, and three command-local stopping Unit cases. Fresh builds had zero
warnings and errors. All eleven Integration cases failed at the unsupported
session opener; all three Unit cases failed at the unsupported progress
constructor; none skipped. The first session fixture separately proved an
available eligible catalogue and held matching lease before reaching that
unsupported opener. Supplemental severity-info formatting passed all seven
files across Core, Integration, and Unit. This is Red evidence, not Green or
whole-task verification.

The ignored receipt `artifacts/task20-supplement-red/prefreeze.json` records
all source/configuration bytes, exact commands and logs, and rejected evidence.
Its prerequisite corrections are retained because they distinguish a meaningful
Red failure from a fixture or execution failure:

- Missing same-worktree Integration assets required a locked restore from the
  existing local package cache, preserving package and audit configuration.
- At `RecoveryDeletionSessionWorkspace.cs(56,26)` and `(56,29)`, `CS1503`
  reported that `int` and `System.IO.FileAttributes` could not convert to
  `System.DateTime`. The literal correction was
  `Assert.Equal(0, File.GetAttributes(candidate.Path) & mask)` to
  `Assert.Equal((FileAttributes)0, File.GetAttributes(candidate.Path) & mask)`;
  the actual fixture uses the explicit ordinary-file attribute mask.
- Recreating a manifest in `ZipArchiveMode.Update` moved it after payload
  entries. The strict reader correctly reported `Malformed`. The corrected
  owned fixture writes the manifest first, preserves payload bytes and order,
  and proves the changed bundle remains `Valid` before exercising the session.
- The new fixture's tuple loop adopted the `IDE0042` deconstruction suggestion.
  A matrix run made against a preceding binary and the matrix with the malformed
  fixture were rejected and replaced by the fresh eleven-case result above.

These are prefreeze evidence corrections and consume no review or grouped
correction budget. Every original Red byte remained unchanged at this freeze.
The original Unit subtree is `b329bba733b6f2318478502f85dbf04301db6805`,
Integration subtree `4e0b83a1eb9bebfc48c8de12b59504356ac7aecd`, public process
test blob `4900ef533db49a3eb509934592b4f8431822bd98`, and public fixture blob
`183d99ab1afdbf06ad781e5206f39d520d1d0809`.

The Overseer subsequently authorized two narrowly bounded corrections at the
earliest invalidated Gray/evidence boundary. They change no public contract,
deletion authority, public journey, phase horizon, or review budget:

1. Invalid ingress without an established workspace needs an empty
   `NotEstablished` plan with a truthfully nullable request. Only this empty
   state may omit the request; ordinary plan creation retains null rejection
   and every normal request invariant. It grants no deletion or effect
   authority. Result mode is `NotEstablished` only for that absent-request
   state. The initial original Red exception permits positive `Assert.NotNull`
   assertions and compiler-proven locals at the existing request dereferences
   in `CleanupTestData.cs` (former lines 325/371) and
   `CleanupResultContractTests.cs` (former lines 237/242). Every original action,
   assertion, and oracle remains intact. Exact old/new patches and predecessor
   identities must accompany the correction freeze, with focused invalid-ingress
   proof. Fabricated workspaces, throwing compatibility accessors, and warning
   suppression are excluded.
2. An unavailable draft may be represented only as blocked preservation with
   consistent exact path, kind, and provenance facts. It gains no deletion
   eligibility. The original unsafe-draft Integration oracle remains
   `unavailable/non-ordinary/blocked/preserve`; the existing Verified and
   Malformed draft rejection remains unchanged.

The same continuous author owns these corrections, their focused rebuild and
refreeze, and subsequent Green. All other original Red bytes remain protected.

The authorized correction boundary is frozen at
`75851d823275aec8fe365a129bc3ffbde96d0f1c`, tree
`3cb56bd3c9ba3b31bcd2613f025d6a989196dba3`. The fresh Unit build passed with
zero warnings and errors; the original 31 Cleanup Unit cases and two new
representation-boundary cases passed with zero failures or skips. The exact
patch, old/new SHA-256 values, original Red blobs, and full source/configuration
manifest are recorded in `artifacts/task20-gray-corrections/exact.patch` and
`receipt.json`. Inspection confirmed that the two original Unit files changed
only through the authorized positive-nullness assertions and locals. This
establishes the corrected representation boundary; session and operation Green,
whole-task formatting, Integration, public, and full gates remain outstanding.

Whole-task severity-info verification subsequently exposed sixteen inherited
original-Red diagnostics and one consequent static-member diagnostic. The
Overseer authorized only their literal
conformance corrections across ten original test files:

- Unit `CleanupPlanningContractTests.cs` and `CleanupResultContractTests.cs`:
  the two `IDE0034` typed-default simplifications.
- Unit `CleanupDefinitionsAndBindingContractTests.cs` and `CleanupTestData.cs`:
  seven `IDE0301`/`IDE0305` empty or spread collection-expression corrections.
- Integration `CleanupJsonAssertions.cs` and `CleanupIntegrationWorkspace.cs`:
  the two `IDE0305` array collection-expression corrections.
- Integration `CleanupApplicationIntegrationTests.cs` and
  `CleanupCatalogueIntegrationTests.cs`: two `xUnit2024` corrections replace
  `Assert.True(mask != 0)` with `Assert.NotEqual((FileAttributes)0, mask)` for
  the unchanged `File.GetAttributes(path) & FileAttributes.ReparsePoint`
  expression. Each retains the identical single attribute read and
  alias-preservation oracle.
- Public `PublishedCleanupWorkspace.cs`: `IDE0071` format-specifier
  interpolation and two `CA1822` static-member corrections. The `HashFile`
  correction permits exactly eleven corresponding receiver qualifications in
  `PublishedCleanupProcessTests.cs`. Making `NestedSentinelStateKey` static
  then exposed `CA1822` on the now-stateless `AssertNestedSupportSnapshot`
  helper. Its authorized closure makes that helper static and qualifies exactly
  five corresponding calls, retaining arguments, assertions, and read order.

This exception preserves values, evaluation and enumeration order, assertions,
actions, and exactly three public journeys. No broader formatting or warning
suppression is authorized. Preimage hashes, the exact correction patch, and a
separate refreeze precede fresh gates. Two owned Gray `CA2208` corrections
replace local/member expressions with the actual `code` and `snapshot`
parameter names. These inherited conformance corrections consume no `T20-R1`
or `T20-C1` unit. All original evidence remains available at its immutable Red
commit; every original test change outside the listed exceptions is prohibited.
The initial complete machine-readable report inventory contains two owned Core
diagnostics, zero Root diagnostics, nine original Unit diagnostics, four
original Integration diagnostics, and three original public-fixture
diagnostics. Diagnostic parsing includes every identifier, including the
lowercase `xUnit2024` entries omitted from the first textual summary.

The conformance boundary is frozen at
`907df556e5a784ff9049c7adaae5e3f35fc6fecf`, tree
`510ddb1d19e91baf717a7d2ab77155cd09bda140`: exactly the ten authorized
original test files and two owned Gray files. Inspection confirmed the literal
patch preserves all existing values and oracles, including the eleven hash
helper and five snapshot helper call qualifications. Preimages, exact test and
Gray patches, the complete 1,996-file source/configuration manifest, and unchanged
supplemental-test identities are in `artifacts/task20-conformance-refreeze`.
Final repository-relative severity-info verification passed Core, Root, Unit,
Integration, and EndToEnd with empty diagnostic reports. Intended include
inventories are respectively 25, 3, 10, 10, and 2 files; diagnostic logs establish
actual project analyzer execution without equating total workspace document
counts with include counts. A fresh source-stable build and behavioral gates
remain required after this correction.

## Focused Green Candidate

Green is frozen at `e339e46143891f0f4dc8f6891636494b16b9bcd9`, tree
`867f6da2f5034d081714504ea27758a8f65166af`. It implements the complete
command-private catalogue, plan, leased application, monotonic result and
residual projection, concrete rendering, and explicit root composition with
the accepted neutral opaque session. Existing single-candidate guard control
flow and deletion/absence helpers were mechanically compared with their
predecessor; only the accepted helper access and qualification changed.

Fresh Release build and focused execution passed with zero warnings, errors,
failures, or skips: 246 Unit, 110 Integration, and exactly three public cases.
Every selector's discovered count matched execution. The focused totals include
38 Cleanup Unit cases, 14 original Cleanup Integration cases, eleven session
cases, and direct recovery, guard, lease, Status, Doctor, Repair, and Extension
Remove regressions. All fifty conditional-expression locations were inspected
against C# design rule 21, including invocation arguments. Final formatting and
`git diff --check` were clean.

`artifacts/task20-focused-green/focused-green-receipt.json`, SHA-256
`d60dc2c360ef35c8032234a8e4d0948be87480c0421bee67806ac7eb37e092c6`, binds
the exact commands and selections, 1,996 source/configuration files, 316 fresh
artifact identities, conformance coverage, and original/supplemental protection
proofs. The precommit build inputs are byte-identical to the committed source.
The receipt explicitly excludes earlier stale, zero-selection, and failing
execution results from this final focused claim.

Author-detected Green rework is retained rather than described as first-pass
success. Two separately frozen Unit assertions failed before correction when
unobserved planned paths were incorrectly described as retained. The corrected
existing unknown representation states that no deletion was attempted and
retains only positively observed paths. A new Green `CA2208` local-name issue
was corrected through a parameter-based lease-state projection. The first
public execution passed two cases and failed one because human output omitted
the frozen `Removed and verified` label; only the production label changed,
then fresh execution passed all three. A broad guard-consumer wildcard selected
zero cases and was rejected in favor of exact source-derived classes.

Complete managed and `linux-x64` native gates used this immutable source and
its same-worktree fresh artifacts. The continuous author remains available for
the one grouped correction after `T20-R1`.

## Complete Gates And Review Boundary

All required execution gates passed against source candidate `e339e461` with
zero failures, skips, or warnings:

| Gate                                 | Executed |
| ------------------------------------ | -------: |
| Managed Unit                         |     2017 |
| Managed Integration                  |     1070 |
| Managed EndToEnd                     |      193 |
| Native Integration, `linux-x64`      |     1070 |
| Native EndToEnd, `linux-x64`         |      193 |
| Managed EndToEnd against native root |      193 |

The native root, Integration, and EndToEnd publications succeeded and produced
ELF executables. Root version output and its marker both report `0.0.0-dev`.
The managed-on-native gate rebuilt EndToEnd with the explicit `linux-x64`
target before execution. All commands used this worktree's artifacts with
`--no-restore`; no global executable, network restore, or other platform was
used. Managed test commands used `--no-build` only after the verified fresh
build and artifact precheck. Native publishes followed the repository CI's
project, runtime, output, and development-publish settings.

`artifacts/task20-full-gates/receipt.json`, SHA-256
`6ca5a58468cbf4d97d9e2423a3c7363197d441a3032210cc93f6430a80a4add4`,
records all eleven literal commands, logs and hashes, positive execution counts,
version/ELF smoke, and final native artifact identities. Source/configuration
drift before and after execution was empty; all 316 focused artifact identities
matched before the full gates. The Task owner independently verified the
receipt, every command's log hash, zero-warning/failure/skip facts, and native
artifact bytes. This proves the current local `linux-x64` boundary; other
platforms remain outside this local receipt.

Current-source prose reconciliation at `1fc542a1` changed only the Cleanup
contract-set status, accepted neutral-session design, and this task's evidence
record. Source and test bytes remain identical to `e339e461`. Before staging,
all 2,701 tracked/current files were hashed and zero untracked files were
accounted for in `artifacts/task20-candidate/prestage-all-files.json`.

The single `T20-R1` reviewer completed read-only preload of current authority,
the complete C# trio, and immutable candidate context while the native gates
ran. Semantic review was released against the complete verified candidate
and both focused/full receipt packets. It includes behavior, C# conformance,
production structure and placement, evidence credibility and sufficiency, and
the changed current-source prose. Its disposition and the subsequent C1
acceptance are recorded below.
The same reviewer owns this one budget unit; no additional topic, council,
or reviewer is selected.

## R1 Disposition And One Grouped C1

`T20-R1` returned `CHANGES_REQUIRED` against `fc1ec902`, tree
`44522b2a56c225ede702ae6294ef1601926e8b0e`. The owner and Overseer
revalidated both exact source/contract traces and accepted the corrections.
The full packet is `artifacts/task20-review/r1.json`:

- `T20-R1-001`, P1 deletion safety: lexical recovery-bucket admission and
  final-component-only checks permit a static bucket or ancestor directory
  link to expose an external ordinary exact-name draft. The same complete
  catalogue and held lease do not establish physical containment. The repair
  uses existing physical-path capability at Cleanup and the new session before
  enumeration and at session validation/deletion. Static links and relevant
  between-stage changes must preserve external targets. Existing single-candidate
  guard and shared catalogue producer policy remain protected. Earliest
  invalidated boundary: Green recovery integration and supplemental safety
  evidence.
- `T20-R1-002`, P2 human-result completeness: human views omit newly observed
  preserved candidates and fail to state the application-lease/final-validation
  contingency of a dry-run deletion. The repair projects those existing typed
  facts in both human views without renderer IO. Earliest invalidated boundary:
  Green human projection and focused evidence.

Separately, `T20-GATE-001` is an owner verification omission, not an R1 finding.
The missing whole-task `max_line_length = 200` scan found thirteen overlong
lines: three new production lines and ten attribute-list lines in four frozen
added test files. The Overseer authorized only newline/indentation changes to
those ten attribute-list lines in `RecoveryBundleDeletionSessionIntegrationTests`,
`CleanupDeletionProgressTests`, `CleanupUnattemptedEffectsTests`, and
`CleanupUnestablishedFactsTests`. Every token, string, assertion, and journey
remains unchanged; exact preimages, patches, hashes, and refreeze are required.
The production lines are ordinary C1 conformance work. No additional original
Red change is authorized.

The owner closed the remaining pre-C1 inventory with
`artifacts/task20-candidate/static-path-pattern-inventory.json` and
`callable-protected-inventory.json`: all fifty C# paths are within the accepted
ownership set; no configuration, project, package, or JavaScript path changed;
the complete fourteen-file Gray comparison has ten unchanged files and four
authorized changes. The bounded prohibited-pattern screen has no production
match; its two test `null!` matches are unchanged original negative
`ArgumentNullException` oracles. The inventory links the existing exact
single-candidate and evidence-protection proofs and states the limits of a
lexical screen. `line-length-inventory.json` retains the failed owner gate.

One coherent `T20-C1` was released to the same continuous author. Decisive
lower-tier failing filesystem and renderer evidence was required before
behavior correction, with fixture prerequisites qualified. All earlier Red
oracles and exactly three public journeys remain protected. Final verification
was required to include current whole-task static, callable/protected, prohibited-pattern,
line-length, severity-info, and conditional-expression coverage; fresh focused,
full managed, and `linux-x64` native gates; and targeted revalidation of the
accepted finding IDs. No second review unit, correction cycle, or council is
authorized.

### C1 Evidence Boundary

The authorized supplemental layout patch is frozen at
`b3e3d87ed822d66c24be1e5d95af60c297d60805`, tree
`11e2f98f58fd1dd0df8ccdbd2d020aa6d84bb512`; exact preimages and the
newline/indentation-only patch are in `artifacts/task20-c1-layout`.

Decisive C1 evidence is frozen at
`4dd94b67f92c7b22420989ea33bd8189829da019`, tree
`0658ec1cd84c11eed33d0f3ae785ce658655962c`.
`artifacts/task20-c1-red/refreeze.json` pins all four new paths and 2,000
source/configuration inputs. Seven real-filesystem Integration cases failed
against the uncorrected behavior or the explicitly unsupported new ancestor
callable; four Unit renderer cases failed on missing observed paths or dry-run
contingency in both human views. Discovery and execution counts matched, with
zero skips and clean prerequisite builds.

The initial linked-storage fixture failed during ownership/teardown and those
receipts were rejected. Its corrected prerequisite-qualified case reached the
old operation and proved deletion of the external ordinary exact-name draft,
then completed teardown. New renderer evidence also required prefreeze
corrections: CS0051 replaced an internal `CliView` public theory parameter with
`bool expanded` and local view selection; CS0103 added the missing Cleanup
namespace import. New ancestor evidence received IDE0300 and CA1822 corrections
before its final rebuild and failing run. These are retained author rework,
not clean first-pass evidence or additional budget units.

The subsequent new storage-boundary implementation produced CA2208 at
`RecoveryDeletionStorageBoundary.cs:72`: the default-state exception used
`nameof(component)` for a local observation. The author changed that argument
to `nameof(directory)`, the actual method parameter, before the fresh build.
`artifacts/task20-c1-green/core-parameter-correction.json` retains the exact
diagnostic and literal correction; no frozen evidence changed.

### Focused C1 Candidate

The same author froze the grouped source correction at
`d6edbd538be98008b3f89c6c0eb8e200a48dec5a`, tree
`7a4085c6a3ccb2d9cf54057fe9d42a7755d7f21d`.
`artifacts/task20-c1-green/focused-c1-receipt.json`, SHA-256
`7a94d02a5b5dd6c4d5718db0ed119ac918f9c866404825abd2054b29e0627235`,
binds 2,000 source/configuration files, 316 artifact identities, and the current
focused and static receipts. The owner independently checked all nine receipt
pins, every artifact, and every source/configuration file against both current
bytes and the immutable candidate.

The corrected operation and opaque session check the physical recovery-directory
chain before and after catalogue observation, then again at held-session
candidate validation and deletion. Existing single-candidate guard, catalogue,
storage, producer, and lease implementation bytes remain unchanged during C1.
Human rendering retains additional observed paths and their established or
unknown disposition in both views, and states the application lease and final
validation required for planned dry-run deletions. It consumes typed facts only.

The Release solution build passed with zero warnings/errors. Focused execution
passed 250 Unit, 117 Integration, and the original three public cases, including
the seven new storage cases and four renderer cases. Every selected case was
discovered and executed, with zero failures/skips/warnings. All five current
repository-relative severity-info checks passed: Core 26, Root 3, Unit 11,
Integration 12, and public 2 paths. The linked static, callable/protected,
prohibited-pattern, conditional-expression, and evidence-protection receipts
cover all 54 task C# paths. The owner independently confirmed zero lines above
200 characters and a clean diff check. This closes the omitted line-length
check on the corrected source without rewriting the earlier failed gate.

### Final C1 Gates And Acceptance

Fresh full managed and `linux-x64` native gates passed through the same
mechanical executor. `artifacts/task20-c1-full-gates/receipt.json`, SHA-256
`6cc8ad6841570c5fc0a7c246bbe40b18ec65ae430545e7d7e179baa861686130`,
records all eleven successful commands: managed Unit 2,021, Integration 1,077,
and EndToEnd 193; native Integration 1,077 and EndToEnd 193; and managed
EndToEnd against the native CLI 193. Every suite had zero failures/skips and
every command had zero warnings. Root, Integration, and EndToEnd Native AOT
publishes generated native code; all three final artifacts are ELF binaries.
The root version and marker are `0.0.0-dev`. All 2,000 source/configuration
identities remained unchanged before and after execution.

The owner independently verified all eleven log hashes and execution counts,
three native-generation markers, and all three native binary hashes/lengths.
The same original reviewer then returned `PASS` for targeted C1 revalidation:
`T20-R1-001` and `T20-R1-002` are resolved, with no unresolved trace within
either accepted ID. It independently checked frozen C1 evidence, the exact
supplemental whitespace exception, source/configuration identities, focused
logs, and the completed full receipt. The disposition is retained in
`artifacts/task20-c1-review/c1.json`; this is C1 closure, not another review unit.

The owner accepts the corrected task after final whole-task conformance and
protection checks. All 54 C# paths in the actual task delta match the recorded
static inventory; the current conditional-expression inspection, five relative
severity-info checks, zero overlong lines, callable/protected comparisons,
bounded prohibited-pattern screen, and direct source inspection support that
disposition. The C1 protected guard/helper/catalogue/storage/lease, producer,
and root-composition scopes remain unchanged. Earlier invalid receipts,
author rework, and the owner line-length omission remain recorded above.

Milestones 7 and 8 are complete. The continuous author was `cleanup_brilliant`
(GPT-6 Astra/high); deterministic full gates used `cleanup_full_gates`
(GPT-5.6 Luna/max); the sole review and targeted C1 closure used `cleanup_r1`
(GPT-6 Astra/high). No further review, correction, or council was consumed.
Execution evidence covers local Linux x64; other platforms were not exercised.
No global installation or remote publication occurred. Central integration had
not occurred at this owner-acceptance boundary; the receipt below records its
subsequent completion.

## Root Acceptance And Integration

The Overseer accepted final owner candidate
`4be87eaab21080f51eb7e2d753db96cc3ed7e2b2`, exact tree
`ab7e488192b435fdefa0b8d30bf1dc853a6b2327`, and squash-integrated it into
local `develop` at `148d378da376d196de183d9564261658164d9d23`, the same tree.
The candidate changes only prose after final tested source `d6edbd53`; its
final receipt is `artifacts/task20-candidate/final-acceptance.json` in the
Cleanup implementation worktree.

Root freshly verified all 2,705 tracked files by bytes and modes, every path
in the 59-path delta (54 C# and five Markdown files), and exact staged/current
tree equality. The integration checker initially treated an added-file manifest
`oldMode` of `000000` as different from an absent predecessor. Correcting that
receipt parser established all 59 delta identities and all 2,705 input
identities. This was an integration-evidence parsing error, not a candidate
defect, and required no source correction.

Root acceptance includes the final focused 250 Unit, 117 Integration, and
three public cases; full managed 2021/1077/193, native 1077/193, and
managed-on-native 193; three AOT generation and native-identity checks; all
54 C# files at no more than 200 characters per line; and five empty
repository-relative format reports with actual analyzer execution. Failures,
skips, and warnings are zero. Static, protected, callable, and prohibited
scoped gates are complete. Exact integration and this prose closeout do not
invalidate the accepted executable evidence or require duplicate full tests.

The same R1 reviewer resolved `T20-R1-001` (P1, physical recovery ancestor and
bucket links) and `T20-R1-002` (P2, missing human paths and dry-run conditions)
within the single C1. Closure artifact `artifacts/task20-c1-review/c1.json`
has SHA-256
`62f290227f513bc7f3d3452edad5479f530fc775c5c963ae4ced7793931e59b3`.
R1 and C1 remaining budgets are zero. There was no R2.

The Task is complete at phase 5/5, milestone 8/8. The project control ledger
owns completion grace and releases Task 23 to consume exact `148d378d`,
refreeze consumer Gray, and complete final Red before Green. Supported
execution evidence remains Linux x64; publication is not authorized.

## Accepted Preflight Authorities

The accepted packet revalidated these repository-local authorities. Their
fingerprints are evidence for this preparation record, not a second contract
surface:

- Cleanup Interface Contract:
  `fc7ef7da44b0d8d9d45bb33ac30f90b1e213a2ca5e3ca574fbd2b0d6b8e8a361`.
- Cleanup Behavior Contract:
  `d653daf7f92060142dd9e831e823283c43557bf03887397918d8b6f6d2cd4067`.
- Shared CLI Operation Contract:
  `2d83005c17ae9ad7e84d71e36b649bf553e5e28ade47322d900b0f6d63dbb44b`.
- Mutation and Recovery technical design:
  `afe9db18a95de4860e378d70350c7a93125bacbbd449cdadf05c01c70b409a23`.
- C# root directive, technical design, and writing style authorities:
  `31045ebcb02d5bfeee8ba9f3112d307b1f72a2186cda618fbf7d22e7d1d90b53`,
  `76aa8fc7aaaa79d9535998f5557150f3754e3d80520a7a06864b61659373c1a9`,
  `c3fa9d31575e77fedb103ca397f0ccf10ab7236e6edbef1658c7fe36138457cb`.
- Development phase, lifecycle, and current CLI control authorities:
  `0267c6d3386fad2bb22be00f1cbd076534ce33c8f33a45d192dd520311b66aac`,
  `14e8edec07114f99aaead60ff56e1199c0d5a5f1852deed0bafd5b5e36addbba`,
  `9a2439aa8a1bc12f653a06cce71f6ff8b11dcc79cf858e065bd6dd0e3021e7d1`,
  `1d7eeeec8ecf7dccd115130bc7f8c156edad35256ebd70989d195c40900c6dbe`,
  `1b88ced0c7f29ecd5516c32dfb9c5f1b139c0f96369e93e7abfb9790cda50019`,
  `bcbd1d0bb8ed3ad7c9f8b4f7ce57910def6d11a4275a2cf19156acb51c14d924`.

The accepted Cleanup Interface and Behavior Contracts remain the source of
truth for syntax, exact roots, catalogue facts, result statuses, output,
errors, examples, and non-goals. The shared operation contract and mutation
design remain the source of truth for the reusable lease, physical identity,
strict recovery-bundle recognition, bounded deletion, and typed effect facts.
No new public field, selector, result status, or shared abstraction is
introduced by this record.

## Phases And Milestones

The task has exactly five phases and eight milestones:

1. **Preflight and activation** — milestone 1, Preflight and preparation, is
   complete. It revalidates the immutable base, accepted authorities,
   dependencies, protected paths, and scope without changing central control.
2. **Explicit Gray**: milestone 2 is complete through the accepted immutable
   callable boundary above.
3. **Explicit Red**: milestone 3 is complete through the accepted immutable
   evidence boundary above. The three original Red sets remain protected.
4. **One coherent implementation and verification**: milestone 4 implements the
   complete accepted flow after the producer/callable refreeze. Milestone 5
   proves focused, public, complete managed, and supported Native AOT behavior.
5. **Review and acceptance**: milestone 6 performs the fresh whole-task review
   across behavior, production structure, and evidence. Milestone 7 applies one
   grouped correction or records a justified no-op. Milestone 8 records the
   evidence, accepted residuals, and final task disposition.

The completed review and grouped correction retain the stable IDs `T20-R1`
and `T20-C1`. Their sole mutable budget accounting is recorded above; no
additional review or correction unit or council was opened.

## Ownership Boundaries

The command-private Gray ownership set is exactly:

- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/CleanupDefinitions.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/CleanupBinding.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/CleanupOperation.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/CleanupRequestBinder.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/Models/Binding/CleanupBindingModels.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/Models/Planning/CleanupCatalogue.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/Models/Planning/CleanupPlan.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/Models/Request/CleanupRequest.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/Models/Result/CleanupResultEnums.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/Models/Result/CleanupResultFacts.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/Models/Result/CleanupResult.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/Models/Presentation/CleanupJsonDocument.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/Shared/Rendering/CleanupHelpSections.cs`
- `src/cli/core/OpenForge.Cli.Core/Commands/Cleanup/Shared/Rendering/CleanupWireVocabulary.cs`

Gray freezes only the Cleanup-local syntax, binding, unsupported operation
seam, request, catalogue, plan, finite result vocabulary, typed result facts,
ordered JSON graph, help, and wire shape. It implements no filesystem behavior
and does not change producer-owned identity, shared result/status/output, lease,
recovery, or existing single-candidate contracts.

The Red ownership sets are three disjoint sets, exactly as follows:

1. Unit Red owns exactly these paths under
   `src/cli/tests/unit/OpenForge.Cli.Core.UnitTests/Commands/Cleanup/`:
   `CleanupDefinitionsAndBindingContractTests.cs`,
   `CleanupRequestContractTests.cs`, `CleanupPlanningContractTests.cs`,
   `CleanupResultContractTests.cs`, `CleanupJsonSchemaContractTests.cs`,
   `CleanupPresentationContractTests.cs`, and `CleanupTestData.cs`.
2. Integration Red owns exactly these paths under
   `src/cli/tests/integration/OpenForge.Cli.IntegrationTests/Commands/Cleanup/`:
   `CleanupCompositionIntegrationTests.cs`,
   `CleanupCatalogueIntegrationTests.cs`, `CleanupPlanningIntegrationTests.cs`,
   `CleanupApplicationIntegrationTests.cs`,
   `CleanupRevalidationIntegrationTests.cs`,
   `CleanupStatusDoctorIntegrationTests.cs`, `CleanupJsonAssertions.cs`, and
   `CleanupIntegrationWorkspace.cs`.
3. Public Red owns exactly
   `src/cli/tests/end-to-end/OpenForge.Cli.EndToEndTests/PublishedCleanupProcessTests.cs`
   and
   `src/cli/tests/end-to-end/OpenForge.Cli.EndToEndTests/PublishedCleanupWorkspace.cs`.

Unit Red owns finite command-local mapping, catalogue, plan, ordering, and typed
result evidence. Integration Red owns real selected-workspace, external-store,
producer, lease, revalidation, preservation, partial-effect, and idempotence
evidence. Public Red owns only the three public journeys below, their process
status, streams, JSON reachability, and supported published-process boundary. A
Red owner does not absorb another Red set or alter an upstream contract to make
evidence pass.

The direct integration neighborhood is root standalone composition and help,
the shared workspace/result/status/output coordinates, producer-owned typed
artifact identities and facts, external recovery-store access,
`WorkspaceLockLease` with `FileShare.None`, strict recovery recognition,
physical identity, the bounded deletion guard, source-generated serialization,
and the existing single-candidate callers. These are integration dependencies,
not additional Gray or Red ownership.

Protected meaning includes all producer-owned `Shared/**`, Task 18 and Task 19
production and test paths, shared public schemas and status/process/stream
mapping, lifecycle publication and mutation, recovery preparation and target
inspection, activity and lock behavior, generated source, package and project
authority, and unrelated user work. Expansion outside the exact sets requires a
new accepted decision; this record authorizes no such expansion.

## Accepted Green-Only Session Decision

The neutral deletion-validation session is accepted for Green-only integration,
not unresolved. The exact boundary is: a held same-workspace lease and a
frozen complete catalogue open one opaque session; the session performs one
exact whole filtered-catalogue comparison; deletion proceeds in ordinal order
with caller-owned candidate selection and immediate per-candidate semantic and
path-kind revalidation. Candidate selection and orchestration remain
Cleanup-owned. Existing single-candidate callers remain unchanged.

Original Red proves catalogue, plan, and revalidation facts, but it does not
deterministically inject a mutation during one apply invocation after frozen
planning with a held lease. The accepted Green boundary supplied direct
real-filesystem integration evidence through the opaque held-lease session,
frozen separately at `af15f1b6`; C1 added the qualified storage-chain evidence
at `4dd94b67`. The original public evidence remains three journeys.

No shared implementation may be introduced before the accepted Task 19 tip and
a fresh callable refreeze. Green may prove this seam only within that boundary;
it may not turn the session into a new public operation, activity mechanism,
producer catalogue, or generic deletion engine.

## Dependencies And Revalidation Triggers

Gray and Red preparation may proceed after Overseer acceptance of this
Preflight packet. Only semantic Green claims and producer-completeness claims
depend on accepted Task 18 “Extension Remove” and Task 19 “Repair” tips plus
fresh revalidation, including closure of every artifact producer that must
publish typed schema-v1 recovery identity and expected-state facts. Cleanup
cannot claim a complete catalogue when a required producer identity or fact is
unavailable. Task 18 and Task 19 remain upstream owners of their own lifecycle
and repair behavior; Cleanup does not duplicate or reverse either command.

Preflight must be re-run and the callable boundary freshly refrozen if any of
the following changes: the Cleanup Interface or Behavior Contract; the shared
operation, result, status, output, or global-flag contract; mutation/recovery,
physical-identity, lease, or deletion-guard authority; Task 18 or Task 19's
accepted tip or callable shape; a producer's identity, provenance, expected
state, or closure; root composition or source-generated serialization; the
immutable base tree; or the accepted Green-only session seam. Such a change
does not activate this task automatically.

## Three Public Cleanup Journeys

Exactly these three simple public journeys are in scope:

1. **Terminal help:** `open-forge cleanup --help` resolves the direct root
   command and exits without workspace or recovery enumeration, lease
   acquisition, or writes.
2. **Default application and repeat:** `open-forge cleanup` selects the exact
   current workspace (or the explicit `--workspace`), removes every eligible
   exact-name final or ordinary draft only within the accepted lease and
   revalidation boundary, preserves unknown or blocked items, verifies every
   effect, and reports residuals. A repeated `open-forge cleanup` forms a fresh
   catalogue and returns a complete empty no-lease no-op when none remains.
3. **Structured dry run:** `open-forge cleanup --dry-run --json` emits one typed
   JSON result from the same complete catalogue, deterministic plan, expected
   state, and preflight as application; it acquires no lease, writes no file,
   and marks each possible effect contingent on application lease acquisition
   and final under-lease validation.

These journeys do not add confirmation, automatic mode, force, age, glob,
operand, selector, prompt, or hidden Doctor/Repair/lifecycle operation.

## Provisional Evidence (Not Accepted Implementation)

Two provisional commits are retained only as preparation evidence:

- `9f8781f108586758767cd2976659d70bfe10a11a` (`feat(cli): prepare cleanup
planning contracts`) contains only `CleanupDefinitions.cs`,
  `Models/Planning/CleanupCatalogue.cs`, `Models/Planning/CleanupPlan.cs`, and
  `Models/Request/CleanupRequest.cs` from the larger Gray ownership set above.
- `7389818f9eabd45502e4a30f0805dd9b7941961b` (`test(cli): freeze cleanup
catalogue contracts`) contains only `CleanupRequestTests.cs` and
  `Models/Planning/CleanupCatalogueAndPlanningTests.cs` under the Cleanup Unit
  test directory. Those filenames are evidence and are not the accepted Red
  ownership names above.

These commits are not accepted implementation and are not transplantable
wholesale. They cover only a partial request/catalogue/plan shape and focused
planning tests; they do not establish root composition, the public typed result
or serialization graph, application policy, the held lease, the accepted
whole-catalogue session, under-lease revalidation, ordinal deletion and
verification, producer closure, all three public journeys, integration, or
managed and Native AOT evidence. Their snapshots therefore cannot override the
accepted Task 19 semantic-Green dependency, callable refreeze, or
shared-contract boundary.

## Planned Evidence And Stop Conditions

The focused evidence must cover the complete accepted behavior: exact root
grammar and shared flags; selected CWD or workspace and external-store
availability; strict schema-v1 final and ordinary draft recognition; complete,
incomplete, blocked, failed, and interrupted result mapping; deterministic
catalogue and plan ordering; dry-run parity and no writes or lease; lease
contention with no deletion; the one under-lease whole-catalogue comparison;
immediate semantic/path-kind revalidation; exact deletion and absence
verification; monotonic partial failure and residuals; idempotence; hashes and
preservation of lookalikes, symlinks, aliases, authored content, and unsafe
items; no activity metadata; unchanged Doctor and Status behavior; process and
JSON stream mapping; managed execution; and supported `linux-x64` Native AOT.

No build or test is performed by this preparation record. The implementation
must stop before broad filename or age deletion, following a link outside
containment, adding an activity classifier or metadata, deleting without the
held same-workspace lease and final revalidation, introducing the shared seam
outside the accepted Green-only boundary, or cleaning an artifact whose
producer identity is unavailable.

## Expected Outcome

`cleanup` forms an operand-free, default-all catalogue of positively recognized
recovery bundles and drafts associated with the selected workspace. Before lease
acquisition, candidates are unclassified for activity. An empty catalogue is a
verified no-lease no-op. Any deletion requires holding the same-workspace
persistent `WorkspaceLockLease` through `FileShare.None`, then repeating final
catalogue and expected-state revalidation under that lease. Lease contention
causes no deletion. Authored, managed-current, malformed, mismatched, differently
keyed, unknown, and unsafe files remain untouched.

## Architecture

- Consume typed artifact identities from every producer. Do not scan by broad
  filename pattern or age alone.
- Catalogue facts establish strict bundle or draft recognition, selected-workspace
  association, provenance, expected state, and safety without an activity
  classifier.
- `CleanupPlan` lists exact candidate targets, expectations, order, contingent
  dry-run projection, required lease boundary, and verification.
- Application may return an empty no-op without acquiring a lease. Otherwise it
  acquires the persistent same-workspace `WorkspaceLockLease` through
  `FileShare.None`, rebuilds the complete catalogue, and revalidates all expected
  state while holding that lease before deleting anything. Contention prevents
  every deletion.
- Reuse physical identity, strict recovery-bundle recognition, bounded deletion,
  and typed deletion-result facts. Cleanup's explicit support-artifact exception
  is nonrecursive; it never extracts, restores, rolls back, compensates, or
  creates a replacement bundle. It writes no activity classifier, marker, PID,
  journal, or lock metadata.

## Evidence

Cover empty no-lease no-op, recognized selected-workspace bundles and drafts,
pre-lease activity-unclassified reporting, same-workspace lease acquisition,
contention with no deletion, final under-lease catalogue and expected-state
revalidation, unknown/malformed/mismatched lookalikes, symlinks/aliases, dry run,
partial failure, idempotence, preservation hashes, no activity metadata,
Doctor/Status after cleanup, process, and AOT.

## Stop Conditions

Stop before glob-based broad deletion, age-only ownership, following an artifact
link outside containment, adding an independent activity classifier or activity
metadata, deleting without the held same-workspace lease and final revalidation,
or cleaning an artifact whose producer identity is unavailable.
