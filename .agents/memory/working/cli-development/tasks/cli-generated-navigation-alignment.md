---
open-forge:
  description: Align Doctor and Status with current generated navigation while preserving real managed-content drift
  tags: [Memory, Working, Contextual, CLI, Task, Doctor, Status, Alignment]
---

# Generated Navigation Alignment

## Frozen Stage D2

Task 27 continuation, COMPLETE phase 3/3, milestone 3/3, from local develop
`ef7bdb3b`, exact tree `602caf0f73eab37ccf4c21f95ad9b29edb40e4b0`.
D1 is complete and squash-integrated. Its native source candidate `c8d786ed`
passes all six suites: 3,232 Unit, 1,745 Integration in both modes and 117 public
cases in all three modes, zero failures/skips. That was the installed predecessor; D2 now supplies the qualified installed build.
Root works directly in `/tmp/open-forge-cli-refactor-sequential`, branch
`codex/cli-generated-navigation-alignment`.

The user permits alignment fixes, improvements and optimizations while preserving
useful functionality. This stage corrects false currentness reports only. It does
not retire diagnostic kinds, hide warnings, change default views or JSON fields,
or change mutation, ownership, recovery or persisted lifecycle meaning. The
[presentation decisions](cli-presentation-audit.md#presentation-decisions-requiring-user-review)
remain separately pending explicit user review.

## Accepted Alignment And Architecture

Extension Install and Index legitimately regenerate Entries from current authored
topology. Index owns only generated interiors; it does not rewrite the Framework
lifecycle record. Extension Install explicitly preserves that Framework section
and verifies its intended generated topology separately. Doctor and Status both
currently project the old persisted generated fingerprint as currentness, even
when their already-read Routes facts prove that exact region is current.

Use those existing exact typed facts in both result projections. A trusted,
source-valid derived Entries target whose baseline read is Changed may be
reported Current only when the same canonical path has a safely available Current
generated-navigation observation. Ordinary authored targets, missing/unavailable/
blocked targets, invalid source identity and unknown or stale navigation retain
their original states. Preserve the raw lifecycle observation and fingerprint;
no record is rewritten and no unavailable fact is promoted through fallback.
Doctor's mixed-state finding must use the same aligned currentness for its exact
managed set. Real authored drift and real stale/malformed generated regions stay
diagnosed. Existing stale-region Framework findings are not retired in D2.

Both Doctor and Status need this identical neutral currentness rule. Place the
small rule under the existing `Framework/Lifecycle/Operational` scope and keep
command-specific finding/JSON formation at each command's aggregation boundary.
Reuse already-read Routes observations; do not rescan the workspace or introduce
a contributor cycle. No new dependency, parser, schema, project or exceptional
machinery is needed. Ordinary enum checks, ordinal path sets and existing typed
projections suffice.

## Applicability And Frozen Evidence

Incorrect read-only status can misdirect users into unnecessary maintenance. D2
changes diagnosis/projection only. Git reverses implementation; actual test
installs and Index writes happen only in owned temporary workspaces with managed
cleanup. Diagnosis snapshots prove no writes. Preserve the existing source,
containment, interruption, malformed, missing and mutation/recovery controls.
No new malicious same-user or concurrency guarantee is selected.

Freeze public producer-to-consumer Red before production: real Extension Install
to Doctor/Status, Index after an authored child addition, stale generated entries,
and authored Framework divergence. A malformed-marker control must remain
non-successful. The draft outside the source tree is not evidence until compiled
and run; correct fixture issues before recording Red without weakening expected
behavior. JSON and all unrelated operation assertions stay frozen.

The rule is a shared observation/public projection alignment, so full managed and
Linux Native AOT qualification applies, plus required changed-C# checks. Review
exact differences, native dogfood and contracts before squash integration.
Plan/checkpoint updates accompany each frozen stage. No remote effects or helpers.

## Frozen Red Receipt

`fa122d07`: six public cases compiled successfully with no warnings/errors.
Five fail on the intended false attention result; malformed markers remain
diagnosed. Zero skips. Evidence: `artifacts/task27-generated-navigation-alignment/`
`red-build.log`, `red.log`, `red.json`. Public evidence stays byte-identical
while production proceeds in phase 2/3.

## Green And Contract Checkpoint

Frozen public Red is unchanged: all six cases pass after alignment. The shared
rule uses available Framework source, valid target identity, GeneratedRegion,
Changed, and exact current navigation path. Doctor computes mixed state from the
same effective states without modifying raw views. Status retains target identity
and baseline fields. Normal authored targets and missing/unavailable/blocked
read paths still use their existing inspectors. No mutation code changed.

Doctor and Status Behavior/Interface now state this derived-region distinction.
Maintenance's writing boundary and Architecture's neutral shared-fact ownership
were checked; neither needs a new rule. Candidate kinds and default visibility
remain pending user review. Next: required C# checks and complete qualification.

## Review Before Native Freeze

Root reviewed the exact production diff sequentially: one neutral rule has two
actual consumers, uses no new scan or mutable observation, and changes no parser,
serializer, diagnostic catalogue, mutation, ownership or recovery code. Doctor
retains raw-target inspection unless the bounded rule proves derived currentness;
its mixed-set count uses that same rule. Status preserves every target field and
ordering while projecting the aligned state. Existing generated-target controls
cover current/changed/missing/unavailable/blocked; malformed and authored drift
public controls remain intact. Required whitespace/style checks pass. Complete
managed Unit (3,232) and Integration (1,745) pass; public is running.

## Managed Qualification And Candidate Freeze

`npm test` completed successfully: 3,232 Unit, 1,745 Integration, 123 public,
zero failures/skips; build zero warnings/errors. Reports are retained at
`artifacts/delivery/managed-reports-u2lvF1/`; task log is `managed.log`.
Frozen public Red remains byte-identical to `fa122d07`. Changed Markdown links
all resolve; `git diff --check` and changed-C# whitespace/style checks pass.
Phase 3/3, milestone 2/3: commit the reviewed source and run the canonical Linux
native build/test gate without changing HEAD or source during that gate.

## Native Dogfood Receipt

Source `29de40a0` passed 24 direct native invocations in owned temporary
workspaces: fresh Framework, Development, Toolkit and all Extensions; Doctor and
Status after installation; Index after an authored addition; stale Entries; real
authored drift; malformed-marker diagnosis. Fourteen read-only invocations
preserve every workspace and external-state file hash. The malformed-marker
check separately retains its real diagnostic and nonzero result. Receipt and
executable hash: `artifacts/task27-generated-navigation-alignment/native-journey.json`.
Native test-runner compilation and the canonical six-suite gate are still running.

## Qualified Closeout

D2 is COMPLETE phase 3/3, milestone 3/3. Qualified source candidate
`29de40a0de6a55cb6ee2c7c50b64a3d79854046e`; frozen public Red `fa122d07`.
All six managed/native suites pass: 3,232 Unit, 1,745 Integration in both modes,
and 123 public cases in all three modes; zero failures/skips. Reports:
`artifacts/delivery/linux-x64/reports-ZBdBMB/`; retained manifest and direct
native receipts: `artifacts/task27-generated-navigation-alignment/`. Twenty-four
direct native invocations pass; fourteen read-only checks preserve all workspace
and external-state file hashes. The installed CLI is refreshed to this candidate.

Doctor and Status now recognize safely observed current generated Entries after
Extension Install or Index. Authored drift, stale/malformed navigation and
missing/unavailable/blocked evidence retain their checks. No mutation, ownership,
recovery, diagnostic kind, JSON field or default visibility was removed.
Doctor/Status Behavior and Interface documents describe the alignment.

This completion record accompanies the authorized local squash into develop
(title: `Align generated navigation currentness in Doctor and Status`).
The feature branch `codex/cli-generated-navigation-alignment` and ignored
`integration.json` receipt retain the exact feature/squash identities and tree
comparison. D1 predecessor is `ef7bdb3b`. No remote effects.

Next: Doctor presentation and nonduplication, then the remaining presentation
stages in the full 28-command audit. The two proposed changes—retiring seven
redundant diagnostic kinds while preserving candidate evidence, and summarizing
information by default—still await explicit user answers. No such change is
implemented. Preserve useful functionality and continue updating this checkpoint.
