# Archived Open Forge Notes

This archive keeps old design sessions and idea notes for historical reconstruction.

Active continuation should use `.agents/memory/` first.

Archive contents:

- `sessions/` - raw session summaries, handoffs, and the original prompt material
- `ideas/` - old design exploration notes that were useful during framework shaping

Treat these files as historical context, not current truth. If a detail here conflicts with current source files or `.agents/memory/`, prefer the current files unless the user explicitly restores an archived decision.
