# Preserved CLI Test Evidence

These files preserve candidate evidence from the removed replacement CLI. They
are not currently runnable projects and do not define the new source architecture.

Each new implementation Task must map relevant cases to current command contracts
and the new production boundary. Keep, rewrite, split, or remove individual tests
only after that mapping. Do not weaken accepted behavior merely to fit the old
implementation.

The removed production source remains available in Git history at commit
`4b873de`.
