Cannot install: 1 Framework file has changed since they were installed.
Workspace: <workspace>
  Error  <workspace>  Framework files have changed [install.managed-divergence]
         <workspace> has changed since it was installed. Install does not replace changed files.
         use open-forge update
         open-forge update
         Update the existing managed Framework state from a fresh plan.
  Bundled Framework fingerprint: <sha256> (14 assets).
  The existing ownership record was preserved.
  No recovery bundle was required.
  Verification was not requested.
  Update the existing managed Framework state from a fresh plan.
Next: open-forge update
