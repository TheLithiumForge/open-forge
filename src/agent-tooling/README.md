# Agent Tooling

This directory contains repository-only tools that support Open Forge agents. It does not define Framework or Extension authority.

- `review/` provides bounded, immutable Git-object inspection for provider bridges and its focused evidence.
- `agent-projections/` applies Open Forge model policy from authored APM agents to generated Codex agent documents.
