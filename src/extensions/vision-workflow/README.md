# Vision Workflow

Workflow-only extension for turning early intent into a decision-ready direction without prematurely treating candidates as accepted truth.

It depends on `vision-capability`; the selector marks that skill extension as required automatically.

```sh
open-forge extend vision-workflow
```
