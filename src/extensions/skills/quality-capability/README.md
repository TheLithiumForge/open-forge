# Quality Skill

Skill-only extension providing the `quality` skill used by testing, review, refactoring, debugging, and mixed development workflows.

It installs automatically when required and may be selected directly:

```sh
open-forge extend quality-capability
```

The installed `SKILL.md` package remains runtime truth. The manifest only composes installation.
